---
phase: 02-skill-adaptation-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/skill-adaptation-template.md
  - .planning/skill-conversion-checklist.md
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03]

must_haves:
  truths:
    - "Any Claude Code skill can be converted to Paperclip format by following the template"
    - "Interactive prompts are identified and replaced with issue-driven triggers"
    - "The checklist catches all known conversion pitfalls before deployment"
  artifacts:
    - path: ".planning/skill-adaptation-template.md"
      provides: "Reusable Paperclip skill template with all required sections"
      contains: "## Input"
    - path: ".planning/skill-conversion-checklist.md"
      provides: "Step-by-step conversion checklist for any skill"
      contains: "Interactive Pattern Removal"
  key_links:
    - from: ".planning/skill-adaptation-template.md"
      to: "~/.paperclip/instances/default/companies/FourPointZero/agents/{agent}/skills/"
      via: "Template structure matches Paperclip skill file format"
      pattern: "## Rules"
    - from: ".planning/skill-conversion-checklist.md"
      to: ".planning/skill-adaptation-template.md"
      via: "Checklist references template sections"
      pattern: "skill-adaptation-template"
---

<objective>
Create a reusable skill adaptation template and conversion checklist that any executor can follow to convert Claude Code skills into Paperclip agent skills consistently.

Purpose: Phases 5 and 9 will adapt 71 skills across 10 agents. Without a standardised template, each conversion would be ad-hoc, inconsistent, and prone to the interactive-prompt and context-overload pitfalls identified in research. This template is the quality gate for all downstream skill work.

Output: Two files -- a template showing the exact structure of a converted Paperclip skill, and a checklist that catches interactive patterns, missing handover instructions, and naming violations before deployment.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/skill-ownership-matrix.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Paperclip skill file format (from existing brand-identity.md on CMO agent) -->

From ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/skills/brand-identity.md:
```markdown
# Skill: Brand Identity

You own the company's visual identity and brand guidelines. [One-line identity statement]

## Brand Identity Process

1. [Step with sub-bullets]
2. [Step with sub-bullets]
3. Document everything in `docs/BRAND-IDENTITY.md`:
   - [concrete sub-steps]
4. Create initial design tokens if a tech stack exists:
   - [conditional step referencing other docs]

## Rules

- [Guardrail 1]
- [Guardrail 2]
- If a decision requires board input, create an approval request.
```

<!-- Claude Code skill format (from linkedin-post-writer SKILL.md) -->

From ~/.claude/skills/linkedin-post-writer/SKILL.md:
```yaml
---
name: linkedin-post-writer
description: Write high-performing LinkedIn posts...
---
```
Key interactive patterns found in Claude Code skills:
- "Ask: What are you working on?" (contentfpz SKILL.md line 9)
- "If unsure, ask one short question" (contentfpz SKILL.md line 72)
- "ask one clarifying question" (linkedin-post-writer line 361)
- "Ask at most ONE clarifying question" (linkedin-post-writer line 361)
- "offer: Want me to try a different hook?" (linkedin-post-writer line 367)
- allowed-tools: AskUserQuestion (humanizer SKILL.md line 17)
- "When given text to humanize:" (humanizer -- assumes interactive handoff)
- "Present the draft" / "present to the user" patterns

<!-- AGENTS.md skill injection pattern -->

From STACK.md:
```markdown
Skills are injected into AGENTS.md as `Read and follow:` directives.
Each line points to a markdown file in the agent's local skills/ directory.
Comment: <!-- Skills are appended here by modules during company assembly -->
```

<!-- Heartbeat adaptation context -->

From STACK.md:
- Input: Change from "ask the user" to "read from issue description"
- Output: Change from "respond in chat" to "write to workspace file, comment on issue"
- Context: Add $AGENT_HOME references, remove interactive prompts
- Coordination: Add handover instructions (who to @-mention when done)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill adaptation template</name>
  <files>.planning/skill-adaptation-template.md</files>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/skills/brand-identity.md
    - ~/.claude/skills/linkedin-post-writer/SKILL.md
    - ~/.claude/skills/humanizer/SKILL.md
    - .planning/research/STACK.md
    - .planning/research/PITFALLS.md
    - .planning/skill-ownership-matrix.md
  </read_first>
  <action>
Create `.planning/skill-adaptation-template.md` with these exact sections:

**Header section:**
```
# Skill Adaptation Template

**Purpose:** Convert any Claude Code skill into Paperclip agent format.
**Used by:** Phases 5 and 9 executors adapting 71 skills across 10 agents.
**Target location:** ~/.paperclip/instances/default/companies/FourPointZero/agents/{agent-slug}/skills/{skill-name}.md
```

**Section 1: Template Structure**
Show the exact skeleton of a converted Paperclip skill file with these sections and placeholder comments:

```markdown
# Skill: {Skill Display Name}

{One-line identity statement: "You [verb] [what] for [whom/purpose]."}

## Input

Read the issue description for:
- {Primary input the skill needs -- e.g., "topic or content brief"}
- {Secondary input -- e.g., "target audience or channel"}
- {Any constraints -- e.g., "word count, tone, format requirements"}

If the issue description is missing required input, comment on the issue asking for it. Do not guess.

## Process

1. {First step -- always starts with reading/understanding the input}
2. {Core skill steps -- transferred verbatim from the Claude Code source}
3. {Output step -- write deliverable to workspace}
4. {Quality step -- if content-producing, run humanizer pass}

## Output

Save deliverable to: `workspace/{descriptive-filename}.md`
Comment on the issue with a summary of what was produced.

## Handover

When complete, @-mention {next-agent-or-reviewer} for review.
If the deliverable is content, @-mention Technical Writer for humanizer quality gate.

## Rules

- {Guardrails transferred from source skill}
- Never ask interactive questions in comments. If input is ambiguous, state your interpretation and proceed.
- All output in UK English.
```

**Section 2: Bundle Template**
Show the skeleton for a bundled skill file (multiple skills in one file):

```markdown
# Skill Bundle: {Bundle Display Name}

This bundle contains {N} related skills. Use the one matching the issue label or description.

## Skills

### {Skill 1 Name}
{Condensed version of the skill -- process, key rules, output format}

### {Skill 2 Name}
{Condensed version}

## Shared Rules
- {Rules that apply to all skills in the bundle}

## Input
Read the issue description and issue labels to determine which skill to apply.

## Output
Save deliverable to: `workspace/{skill-name}-{descriptive-filename}.md`

## Handover
@-mention {reviewer} when complete.
```

**Section 3: Naming Conventions**
- All filenames: lowercase, hyphens only, `.md` extension
- Colon conversion: `claude-blog:blog-write` becomes `blog-write.md`
- Namespace stripping: `anthropic-skills:content-creator` becomes `content-creator.md`
- Bundle naming: use the bundle name from skill-ownership-matrix.md (e.g., `cro-suite.md`, `blog-engine.md`)
- Fallback naming: `{skill-name}.fallback.md`

**Section 4: Interactive Pattern Replacements**
A two-column table showing exact before/after replacements:

| Claude Code Pattern | Paperclip Replacement |
|---|---|
| `Ask: What are you working on?` | `Read the issue description for the task brief.` |
| `If unsure, ask one short question` | `If input is ambiguous, state your interpretation in an issue comment and proceed.` |
| `ask one clarifying question` | `Comment on the issue requesting the missing detail. Do not block on a response -- proceed with reasonable defaults if possible.` |
| `When the user gives you a topic` | `When the issue description contains a topic` |
| `Present the draft` / `present to the user` | `Save the draft to workspace/ and comment on the issue with a summary.` |
| `offer: "Want me to try a different hook?"` | Remove entirely. Produce the best version. If multiple options are warranted, include them all in the deliverable. |
| `allowed-tools: AskUserQuestion` | Remove from any frontmatter. Not available in headless mode. |
| `Save finished work to the-vault/` | `Save finished work to workspace/` |
| `confirm the filename with the user first` | Remove. Use the naming convention: `{skill-name}-{descriptive-slug}.md` |
| `Run humanizer on any finished content before saving` | `After completing the deliverable, hand off to Technical Writer for humanizer quality gate pass.` |

**Section 5: AGENTS.md Integration**
Show how to add the skill reference to an agent's AGENTS.md:
```
Read and follow: $AGENT_HOME/skills/{skill-name}.md
```
Note: Add this line below the `<!-- Skills are appended here by modules during company assembly -->` comment in the agent's AGENTS.md.

**Section 6: What NOT to Change**
- Core skill logic, frameworks, and domain knowledge transfer verbatim
- Specific examples, templates, and reference data stay intact
- Quality checklists stay intact (but remove interactive verification steps like "read it out loud")
- Voice/tone guidelines stay intact
- Any data tables, hook libraries, structure templates stay intact
  </action>
  <verify>
    <automated>grep -c "## Input" .planning/skill-adaptation-template.md && grep -c "## Process" .planning/skill-adaptation-template.md && grep -c "## Output" .planning/skill-adaptation-template.md && grep -c "## Handover" .planning/skill-adaptation-template.md && grep -c "## Rules" .planning/skill-adaptation-template.md && grep -c "Interactive Pattern Replacements" .planning/skill-adaptation-template.md && grep -c "Bundle Template" .planning/skill-adaptation-template.md && grep -c "Naming Conventions" .planning/skill-adaptation-template.md && grep -c "AGENTS.md Integration" .planning/skill-adaptation-template.md && grep -c "What NOT to Change" .planning/skill-adaptation-template.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Input" returns at least 2 matches (template section + bundle section)
    - grep "## Handover" returns at least 1 match
    - grep "Interactive Pattern Replacements" returns 1 match
    - grep "Ask: What are you working on" returns 1 match (in the before column)
    - grep "Read the issue description" returns at least 2 matches
    - grep "workspace/" returns at least 2 matches (output paths)
    - grep "@-mention" returns at least 2 matches (handover instructions)
    - grep "claude-blog:blog-write.*blog-write.md" returns 1 match (naming convention example)
    - grep "anthropic-skills:content-creator.*content-creator.md" returns 1 match
    - grep "UK English" returns at least 1 match
    - File has more than 80 lines (substantive template, not a stub)
  </acceptance_criteria>
  <done>A complete skill adaptation template exists that shows the exact Paperclip skill file structure, bundle structure, naming conventions, interactive pattern replacements, AGENTS.md integration, and preservation rules. Any executor can use it to convert a Claude Code skill without asking clarifying questions.</done>
</task>

<task type="auto">
  <name>Task 2: Create conversion checklist</name>
  <files>.planning/skill-conversion-checklist.md</files>
  <read_first>
    - .planning/skill-adaptation-template.md (created in Task 1)
    - .planning/research/PITFALLS.md
    - .planning/skill-ownership-matrix.md
  </read_first>
  <action>
Create `.planning/skill-conversion-checklist.md` with the following exact content structure:

**Header:**
```
# Skill Conversion Checklist

**Purpose:** Step-by-step checklist for converting a Claude Code skill to Paperclip format.
**Reference:** See skill-adaptation-template.md for the target file structure.
**Apply to:** Every skill conversion in Phases 5 and 9.
```

**Section 1: Pre-Conversion (before writing anything)**
```
## Pre-Conversion

- [ ] Identify the source skill file path (Claude Code location)
- [ ] Confirm the target agent from skill-ownership-matrix.md
- [ ] Confirm whether this is a standalone skill or part of a bundle
- [ ] If bundle: check if the bundle file already exists (add to it, don't create a duplicate)
- [ ] Determine the target filename using naming conventions from skill-adaptation-template.md
- [ ] Read the source skill file completely before making changes
```

**Section 2: Interactive Pattern Removal (ADPT-02)**
```
## Interactive Pattern Removal

Scan the source file for every pattern in the "Interactive Pattern Replacements" table in skill-adaptation-template.md. Check each one:

- [ ] No "Ask:" or "ask the user" patterns remain
- [ ] No "clarifying question" patterns remain
- [ ] No "If unsure, ask" patterns remain
- [ ] No "present to the user" or "present the draft" patterns remain
- [ ] No "offer:" or "would you like" choice patterns remain
- [ ] No "AskUserQuestion" tool references remain
- [ ] No "confirm with the user" patterns remain
- [ ] No "the-vault/" references remain (replaced with "workspace/")
- [ ] No "save finished work" without specifying workspace/ path

Verification command:
grep -iE "(ask the user|ask:|clarifying question|if unsure.*(ask|question)|present (to the user|the draft)|would you like|AskUserQuestion|confirm.*(with|from) the user|the-vault)" {converted-file}
Expected result: zero matches.
```

**Section 3: Issue-Driven Trigger Logic (ADPT-03)**
```
## Issue-Driven Trigger Logic

- [ ] File has an "## Input" section that reads from issue description
- [ ] Input section specifies what data to extract from the issue
- [ ] If input is missing from issue, the skill comments asking for it (does not guess)
- [ ] No blocking patterns: skill proceeds with reasonable defaults where possible
- [ ] Issue labels referenced where skill selection depends on context (bundles especially)
```

**Section 4: Structure Compliance**
```
## Structure Compliance

- [ ] File starts with "# Skill: {Name}" (standalone) or "# Skill Bundle: {Name}" (bundle)
- [ ] Has ## Input section
- [ ] Has ## Process section with numbered steps
- [ ] Has ## Output section specifying workspace/ path
- [ ] Has ## Handover section with specific @-mention target
- [ ] Has ## Rules section with at least "All output in UK English"
- [ ] Core skill logic preserved verbatim from source (not summarised or watered down)
- [ ] Examples, templates, data tables, and reference material preserved
```

**Section 5: Content Quality Gate**
```
## Content Quality Gate (content-producing skills only)

- [ ] Handover section includes @-mention of Technical Writer for humanizer pass
- [ ] No skill claims to run humanizer itself (only Technical Writer owns humanizer execution)
- [ ] Output format is markdown saved to workspace/
```

**Section 6: Naming and Filing**
```
## Naming and Filing

- [ ] Filename is lowercase, hyphens only, .md extension
- [ ] Colons converted to hyphens or stripped (claude-blog:blog-write -> blog-write.md)
- [ ] Namespace prefixes stripped (anthropic-skills:content-creator -> content-creator.md)
- [ ] File saved to correct agent's skills/ directory: ~/.paperclip/instances/default/companies/FourPointZero/agents/{agent-slug}/skills/
- [ ] AGENTS.md updated with "Read and follow: $AGENT_HOME/skills/{filename}" line
```

**Section 7: Post-Conversion Verification**
```
## Post-Conversion Verification

Run these commands after conversion:

1. Interactive pattern scan (must return 0 matches):
   grep -icE "(ask the user|ask:|clarifying question|if unsure.*(ask|question)|present (to the user|the draft)|would you like|AskUserQuestion|confirm.*(with|from) the user|the-vault)" {file}

2. Required sections present (each must return at least 1):
   grep -c "## Input" {file}
   grep -c "## Process" {file}
   grep -c "## Output" {file}
   grep -c "## Handover" {file}
   grep -c "## Rules" {file}

3. Workspace output path present:
   grep -c "workspace/" {file}

4. AGENTS.md reference added:
   grep -c "Read and follow.*skills/{skill-name}" {agent-AGENTS.md-path}
```

**Section 8: Common Mistakes (from PITFALLS.md)**
```
## Common Mistakes

| Mistake | What Happens | How to Catch |
|---------|--------------|--------------|
| Copy-paste without removing interactive patterns | Agent stalls or hallucinates user responses | Run the interactive pattern grep in Post-Conversion Verification |
| Over-adapting (rewriting the core logic) | Skill loses its value, becomes a generic summary | Compare word count: adapted file should be within 30% of source length for standalone skills |
| Missing handover instructions | Work sits in limbo, nobody picks it up | grep for "@-mention" in the Handover section |
| Wrong agent assignment | Skill conflicts with another agent's skills | Cross-check against skill-ownership-matrix.md before filing |
| Forgetting AGENTS.md reference | Agent never loads the skill | grep AGENTS.md for the skill filename after filing |
| Bundle duplication | Two files contain the same skill | Check if bundle file already exists before creating |
```
  </action>
  <verify>
    <automated>grep -c "Interactive Pattern Removal" .planning/skill-conversion-checklist.md && grep -c "Issue-Driven Trigger Logic" .planning/skill-conversion-checklist.md && grep -c "Structure Compliance" .planning/skill-conversion-checklist.md && grep -c "Post-Conversion Verification" .planning/skill-conversion-checklist.md && grep -c "skill-adaptation-template" .planning/skill-conversion-checklist.md && grep -c "Common Mistakes" .planning/skill-conversion-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Interactive Pattern Removal" returns 1 match (section heading)
    - grep "Issue-Driven Trigger Logic" returns 1 match
    - grep "Structure Compliance" returns 1 match
    - grep "Post-Conversion Verification" returns 1 match
    - grep "skill-adaptation-template" returns at least 2 matches (references to the template)
    - grep "Common Mistakes" returns 1 match
    - grep -c "\- \[ \]" returns at least 20 (minimum 20 checklist items)
    - grep "grep" returns at least 4 matches (verification commands embedded)
    - grep "ADPT-02" or "interactive" (case insensitive) returns at least 1 match
    - grep "ADPT-03" or "issue-driven" (case insensitive) returns at least 1 match
    - grep "workspace/" returns at least 2 matches
    - File has more than 60 lines
  </acceptance_criteria>
  <done>A conversion checklist exists with at least 20 checkbox items covering pre-conversion checks, interactive pattern removal, issue-driven triggers, structure compliance, content quality gate, naming/filing, post-conversion verification commands, and common mistakes. Any executor can apply it to any skill file and verify compliance mechanically.</done>
</task>

</tasks>

<verification>
1. Both files exist in .planning/:
   - ls .planning/skill-adaptation-template.md .planning/skill-conversion-checklist.md
2. Template has all required sections (10 grep checks in Task 1 verify)
3. Checklist has all required sections (6 grep checks in Task 2 verify)
4. Checklist references the template (grep "skill-adaptation-template" returns 2+)
5. No interactive patterns in the template's own examples:
   - grep -icE "ask the user|AskUserQuestion" .planning/skill-adaptation-template.md should return 0 matches outside the "before" column of the replacements table
</verification>

<success_criteria>
- ADPT-01: skill-adaptation-template.md exists with Template Structure, Bundle Template, Naming Conventions, Interactive Pattern Replacements, AGENTS.md Integration, and What NOT to Change sections
- ADPT-02: Interactive Pattern Replacements table in the template lists all known patterns with exact before/after text, and the checklist has a dedicated "Interactive Pattern Removal" section with grep verification command
- ADPT-03: Template's Input section shows "Read the issue description" pattern, and checklist has "Issue-Driven Trigger Logic" section verifying issue-based input
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-adaptation-template/02-01-SUMMARY.md`
</output>
