---
phase: 10-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/phases/10-validation/10-02-SUMMARY.md
autonomous: false
requirements:
  - VALD-04
  - VALD-05
must_haves:
  truths:
    - "Engineer receives a technical issue and follows implementation workflow"
    - "All 10 agents process at least one test issue on heartbeat without stalling"
    - "Each heartbeat completes within a reasonable time (under 10 minutes)"
  artifacts:
    - path: "Paperclip issues"
      provides: "All validation issues in completed or in_progress status"
    - path: "the-vault/validation-*.md"
      provides: "Deliverables from agents that produce content"
  key_links:
    - from: "heartbeat run command"
      to: "agent processing"
      via: "npx paperclipai heartbeat run -a <agentId>"
      pattern: "heartbeat.*run"
---

<objective>
Run heartbeats for all agents and validate they process their assigned test issues correctly. Create remaining test issues for tech stream agents not covered in Plan 01.

Purpose: This is the final validation that the entire 10-agent company works end-to-end. Each agent must pick up its assigned issue on heartbeat, process it using the correct skills, and produce appropriate output.

Output: All 10 agents validated, results documented in summary.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-validation/10-01-SUMMARY.md
</context>

<reference>
Paperclip API at http://localhost:3100
Company ID: c86bff2f-e63b-4982-8a0d-aa4b50fc82a5

Agent IDs (from Paperclip - get fresh list at start):
- CEO: 357971bd-f509-4e8c-a147-b519cf84356d
- CMO: 293ac1cb-098c-4fc7-83dc-3ef8b805c8db
- CTO: 9f63e8ed-328b-4eea-9b2f-30d2c1d42e1d
- Product Owner: f74c5796-2876-496b-b7f1-7f95334ec76f
- Technical Writer: 3ff49ad0-539a-48e9-87fa-63f5ebcc0509
- Customer Success: 6003d629-f007-49f6-8530-1168f18f2159
- UX Researcher: c25043f9-85a7-4fbd-8025-ee57b92a2fe0
- Engineer: 97307b6f-d9e6-4c84-ab46-512084c15862
- Code Reviewer: 8df70cb1-83ea-4f81-8ade-80cf0c8a8bef
- LinkedIn Growth Director: <GET FROM 10-01-SUMMARY.md>

CLI commands (always prefix with `source ~/.zshrc 2>/dev/null;`):
- Heartbeat: npx paperclipai heartbeat run -a <agentId>
- Create issue: npx paperclipai issue create -C "$COMPANY_ID" --title "..." --description "..." --priority medium --assignee-agent-id <agentId>
- List issues: npx paperclipai issue list -C "$COMPANY_ID"
- Get issue: npx paperclipai issue get <issueId>

IMPORTANT: Heartbeats spawn a Claude process that can take 2-10 minutes. The CLI streams logs. Wait for each heartbeat to complete before assessing results. Only run ONE heartbeat at a time (machine resource constraint from Out of Scope: "2-3 concurrent" max).
</reference>

<tasks>

<task type="auto">
  <name>Task 1: Create remaining test issues and run all heartbeats</name>
  <files>No files modified - Paperclip API operations only</files>
  <read_first>
    - Read .planning/phases/10-validation/10-01-SUMMARY.md for agent IDs (especially LinkedIn Growth Director)
    - Run `source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"` to get fresh agent statuses
    - Run `source ~/.zshrc 2>/dev/null; npx paperclipai issue list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5" --match "VALIDATION"` to see existing validation issues
  </read_first>
  <action>
    IMPORTANT: All CLI commands must be prefixed with `source ~/.zshrc 2>/dev/null;` to load nvm/node.

    COMPANY_ID="c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"

    PHASE A: Create remaining test issues for agents not covered in Plan 01.

    Issue 5 - Engineer Implementation Test (VALD-04):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Create a simple markdown-to-PDF conversion script" \
      --description "This is a validation test issue. The Engineer should follow the implementation workflow from HEARTBEAT.md: (1) Assess the task, (2) Plan the approach using document-tools skill reference, (3) Create a minimal Python script at the-vault/validation-engineer-md2pdf.py that converts a markdown string to PDF using the approach from the pdf skill. The script does not need to actually run - just demonstrate the Engineer follows the implementation workflow and references the correct skill. Save any planning notes to the-vault/validation-engineer-notes.md" \
      --priority medium \
      --assignee-agent-id "97307b6f-d9e6-4c84-ab46-512084c15862"
    ```

    Issue 6 - CTO Oversight Test (VALD-05):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Review the tech stack documentation and identify gaps" \
      --description "This is a validation test issue. The CTO should demonstrate engineering oversight by reviewing the current FPZ tech stack (Next.js, Vercel, Notion, n8n) and producing a brief gap analysis. Expected output: A short document listing 3-5 areas where documentation or standards could be improved. Save to the-vault/validation-cto-tech-review.md" \
      --priority medium \
      --assignee-agent-id "9f63e8ed-328b-4eea-9b2f-30d2c1d42e1d"
    ```

    Issue 7 - Product Owner Backlog Test (VALD-05):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Prioritise the current issue backlog" \
      --description "This is a validation test issue. The Product Owner should review all current issues and produce a prioritised backlog summary. Expected output: List current issues by priority with a brief rationale for ordering. Save to the-vault/validation-po-backlog-priority.md" \
      --priority medium \
      --assignee-agent-id "f74c5796-2876-496b-b7f1-7f95334ec76f"
    ```

    Issue 8 - Customer Success Test (VALD-05):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Build a brief competitive snapshot of AI recruitment firms" \
      --description "This is a validation test issue. Customer Success should use competitor-alternatives skill to produce a brief competitive snapshot of 3 AI-focused recruitment competitors. Save to the-vault/validation-cs-competitive-snapshot.md" \
      --priority medium \
      --assignee-agent-id "6003d629-f007-49f6-8530-1168f18f2159"
    ```

    Issue 9 - UX Researcher Test (VALD-05):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Run a quick CRO audit on the FPZ homepage concept" \
      --description "This is a validation test issue. UX Researcher should use cro-suite skills to produce a brief CRO audit of a hypothetical FPZ landing page. Expected output: 5 CRO recommendations for a recruitment firm landing page. Save to the-vault/validation-ux-cro-audit.md" \
      --priority medium \
      --assignee-agent-id "c25043f9-85a7-4fbd-8025-ee57b92a2fe0"
    ```

    Issue 10 - Code Reviewer Test (VALD-05):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Review a sample PR for code quality standards" \
      --description "This is a validation test issue. Code Reviewer should demonstrate its review workflow by reviewing this sample code snippet and producing review feedback:\n\nSample code:\nfunction getUser(id) {\n  var user = db.query('SELECT * FROM users WHERE id = ' + id);\n  if (user) { return user; }\n}\n\nExpected: Identify SQL injection, missing error handling, var usage, missing return type. Save review to the-vault/validation-cr-code-review.md" \
      --priority medium \
      --assignee-agent-id "8df70cb1-83ea-4f81-8ade-80cf0c8a8bef"
    ```

    PHASE B: Run heartbeats one at a time. Run each agent's heartbeat and wait for completion before starting the next. Order: CEO first (it may delegate), then remaining agents.

    IMPORTANT: Only run ONE heartbeat at a time. Each heartbeat spawns a Claude process that uses significant resources.

    For each of the 10 agents, run:
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai heartbeat run -a "<AGENT_ID>" --timeout-ms 600000
    ```

    Recommended order:
    1. CEO (357971bd) - test delegation routing
    2. CMO (293ac1cb) - test content production
    3. Technical Writer (3ff49ad0) - test quality gate
    4. LinkedIn Growth Director (<ID from 10-01>) - test post + outreach
    5. Engineer (97307b6f) - test implementation workflow
    6. CTO (9f63e8ed) - test tech oversight
    7. Product Owner (f74c5796) - test backlog management
    8. Customer Success (6003d629) - test competitive intel
    9. UX Researcher (c25043f9) - test CRO audit
    10. Code Reviewer (8df70cb1) - test code review

    After each heartbeat, check the issue status:
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue list -C "$COMPANY_ID" --match "VALIDATION"
    ```

    Record results for each agent: did it pick up the issue? Did it produce output? Did it stall?

    If an agent stalls (heartbeat runs but issue stays in same status after 10 minutes), record it as a failure and move to the next agent. Do NOT spend time debugging - just document what happened.

    PHASE C: After all 10 heartbeats complete, produce a validation results summary. Check:
    - Which issues moved from todo/backlog to in_progress or completed
    - Which agents produced deliverables in the-vault/
    - Which agents stalled or errored
  </action>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; npx paperclipai issue list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5" --match "VALIDATION" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 10 VALIDATION issues exist (4 from Plan 01 + 6 new)
    - Each of the 10 agents had a heartbeat triggered
    - Issue list shows which issues were processed (status changed from todo/backlog)
    - Results documented for each agent
  </acceptance_criteria>
  <done>All 10 agents have been heartbeat-triggered with their validation issues. Results captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review validation results and confirm company works</name>
  <files>None - visual verification in Paperclip UI and the-vault/ directory</files>
  <action>
    Present the user with the verification steps below. Wait for their confirmation before marking this plan and phase complete.
  </action>
  <what-built>Created test issues for all 10 agents and ran heartbeats for each. Agents processed their assigned validation issues.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 and check the Issues page
    2. Look at each VALIDATION issue - check its status and any comments/activity
    3. Check the-vault/ directory for any deliverables produced by agents
    4. Review the validation results in the executor's output
    5. Key things to verify:
       - CEO: Did it delegate the blog post issue to the CMO stream?
       - CMO: Did it produce a content calendar?
       - Technical Writer: Did it run the quality gate and clean up the AI-sounding text?
       - LinkedIn Growth Director: Did it produce a post and outreach sequence?
       - Engineer: Did it follow the implementation workflow?
       - All 10 agents: Did each one pick up its issue without stalling?
  </how-to-verify>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; ls /Users/martynmakinson/Documents/fourpointzero/the-vault/validation-*.md 2>&1 | wc -l | tr -d ' '</automated>
  </verify>
  <done>User confirmed the 10-agent company is operational. Phase 10 validation complete.</done>
  <resume-signal>Type "approved" if the company is working, or list which agents failed and need investigation.</resume-signal>
</task>

</tasks>

<verification>
All 10 agents processed at least one test issue on heartbeat. Results documented in summary.
</verification>

<success_criteria>
- All 10 agents had heartbeats triggered
- CEO delegated content work to the correct stream
- CMO produced a deliverable using content-strategy skill
- Technical Writer ran a quality gate pass on sample content
- Engineer followed the implementation workflow
- LinkedIn Growth Director produced a post and outreach sequence
- No agent stalled permanently (all either completed or produced partial output)
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation/10-02-SUMMARY.md`
</output>
