---
phase: 10-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-validation/10-01-SUMMARY.md
autonomous: false
requirements:
  - VALD-01
  - VALD-02
  - VALD-03
  - VALD-06
must_haves:
  truths:
    - "All 8 pending agents are approved and can receive work"
    - "LinkedIn Growth Director agent exists in Paperclip"
    - "CEO has a delegation test issue assigned"
    - "CMO has a content production test issue assigned"
    - "Technical Writer has a quality gate test issue assigned"
    - "LinkedIn Growth Director has a post+outreach test issue assigned"
  artifacts:
    - path: "Paperclip agents"
      provides: "All 10 agents in approved/idle status"
    - path: "Paperclip issues"
      provides: "4 test issues created and assigned"
  key_links:
    - from: "test issues"
      to: "agent assignments"
      via: "assigneeAgentId field"
      pattern: "assignee.*agent"
---

<objective>
Pre-flight checks and test issue creation for business stream agents (CEO, CMO, Technical Writer, LinkedIn Growth Director).

Purpose: Before running heartbeats, agents must be approved and test issues must exist. 8 of 10 agents are currently in pending_approval status. The LinkedIn Growth Director agent does not yet exist in Paperclip (only 9 agents created). This plan fixes both blockers and creates the first wave of test issues.

Output: All agents approved, LinkedIn Growth Director created, 4 test issues assigned to business stream agents.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/skill-ownership-matrix.md
</context>

<reference>
Paperclip API at http://localhost:3100
Company ID: c86bff2f-e63b-4982-8a0d-aa4b50fc82a5

Agent IDs (from Paperclip):
- CEO: 357971bd-f509-4e8c-a147-b519cf84356d (status: running)
- CMO: 293ac1cb-098c-4fc7-83dc-3ef8b805c8db (status: pending_approval)
- CTO: 9f63e8ed-328b-4eea-9b2f-30d2c1d42e1d (status: pending_approval)
- Product Owner: f74c5796-2876-496b-b7f1-7f95334ec76f (status: pending_approval)
- Technical Writer: 3ff49ad0-539a-48e9-87fa-63f5ebcc0509 (status: pending_approval)
- Customer Success: 6003d629-f007-49f6-8530-1168f18f2159 (status: pending_approval)
- UX Researcher: c25043f9-85a7-4fbd-8025-ee57b92a2fe0 (status: pending_approval)
- Engineer: 97307b6f-d9e6-4c84-ab46-512084c15862 (status: pending_approval)
- Code Reviewer: 8df70cb1-83ea-4f81-8ade-80cf0c8a8bef (status: pending_approval)
- Chat Assistant: 066b99e4-74b4-463f-9043-5eac77013e9d (status: idle)
- LinkedIn Growth Director: DOES NOT EXIST YET - must be created

Pending approval IDs (all 8 are hire_agent type):
5e5e1996-a38a-4d3e-879b-58c05d351ea6
d029be03-5d25-4599-8397-baf86fe9e236
1c99f981-aefd-4013-919b-f751723a9bf3
e6757856-38a9-4100-8161-23bfe0278854
a2dd5a8a-b60c-4b4f-91ac-e15f7567ae01
48da63a6-bde0-4993-a6d6-704fff25849d
c516ad14-0623-46a4-822c-6967f5307226
2bc655d1-302c-4159-9e15-4007b26175ff

CLI commands (always prefix with `source ~/.zshrc 2>/dev/null;`):
- List agents: npx paperclipai agent list -C "$COMPANY_ID"
- Approve: npx paperclipai approval approve <approvalId>
- Create issue: npx paperclipai issue create -C "$COMPANY_ID" --title "..." --description "..." --priority medium --assignee-agent-id <agentId>
- List issues: npx paperclipai issue list -C "$COMPANY_ID"
- Heartbeat: npx paperclipai heartbeat run -a <agentId>
- API issue PATCH: curl -s -X PATCH "http://localhost:3100/api/issues/<issueId>" -H "Content-Type: application/json" -d '{"field":"value"}'
</reference>

<tasks>

<task type="auto">
  <name>Task 1: Approve all pending agents and create LinkedIn Growth Director</name>
  <files>No files modified - Paperclip API operations only</files>
  <read_first>
    - Run `source ~/.zshrc 2>/dev/null; npx paperclipai approval list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"` to get current approval IDs
    - Run `source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"` to confirm current agent statuses
  </read_first>
  <action>
    IMPORTANT: All CLI commands must be prefixed with `source ~/.zshrc 2>/dev/null;` to load nvm/node.

    Step 1: Approve all 8 pending hire_agent approvals. For each approval ID from the approval list, run:
    `source ~/.zshrc 2>/dev/null; npx paperclipai approval approve <approvalId>`

    Approve all 8 approval IDs listed in the reference section above. If any approval IDs have changed, use the fresh list from read_first.

    Step 2: After all approvals complete, verify all agents show status=idle (not pending_approval):
    `source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"`

    Step 3: Check if LinkedIn Growth Director already exists. If NOT present in agent list, it needs to be created. The LinkedIn Growth Director agent was configured in Phase 8.5 with AGENTS.md at:
    /Users/martynmakinson/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/AGENTS.md

    To create the agent via API:
    ```
    source ~/.zshrc 2>/dev/null; curl -s -X POST "http://localhost:3100/api/companies/c86bff2f-e63b-4982-8a0d-aa4b50fc82a5/agents" \
      -H "Content-Type: application/json" \
      -d '{
        "name": "LinkedIn Growth Director",
        "role": "general",
        "title": "LinkedIn Growth Director",
        "icon": "linkedin",
        "reportsTo": "357971bd-f509-4e8c-a147-b519cf84356d",
        "adapterType": "claude_local",
        "adapterConfig": {
          "cwd": "/Users/martynmakinson/Documents/fourpointzero",
          "model": "claude-sonnet-4-6",
          "instructionsFilePath": "/Users/martynmakinson/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/AGENTS.md",
          "dangerouslySkipPermissions": true
        },
        "runtimeConfig": {
          "heartbeat": {
            "enabled": true,
            "intervalSec": 3600,
            "wakeOnDemand": true
          }
        }
      }'
    ```

    If agent creation requires board approval, approve it immediately using the approval CLI.

    Step 4: Verify final agent list shows all 10 configured agents (plus Chat Assistant = 11 total) and none are in pending_approval.
  </action>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5" 2>&1 | grep -c "pending_approval" | grep -q "^0$" && echo "PASS: No pending approvals" || echo "FAIL: Still have pending approvals"</automated>
  </verify>
  <acceptance_criteria>
    - Zero agents in pending_approval status
    - All 10 configured agents visible (CEO, CMO, CTO, Product Owner, Technical Writer, Customer Success, UX Researcher, Engineer, Code Reviewer, LinkedIn Growth Director)
    - LinkedIn Growth Director agent exists with correct instructionsFilePath
  </acceptance_criteria>
  <done>All 10 agents approved and operational. LinkedIn Growth Director exists in Paperclip.</done>
</task>

<task type="auto">
  <name>Task 2: Create and assign test issues for business stream agents</name>
  <files>No files modified - Paperclip API operations only</files>
  <read_first>
    - Run `source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"` to get fresh agent IDs (especially LinkedIn Growth Director's new ID)
  </read_first>
  <action>
    IMPORTANT: All CLI commands must be prefixed with `source ~/.zshrc 2>/dev/null;` to load nvm/node.

    Create 4 test issues using the Paperclip CLI. Each issue tests a specific agent capability from the VALD requirements.

    COMPANY_ID="c86bff2f-e63b-4982-8a0d-aa4b50fc82a5"

    Issue 1 - CEO Delegation Test (VALD-01):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Write a blog post about AI hiring trends in VFX studios" \
      --description "This is a validation test issue. The CEO should recognise this as content work and delegate it to the CMO stream. Expected behaviour: CEO reads this issue, identifies it as content production (blog post), and either reassigns to CMO or creates a sub-issue for the CMO. Do NOT actually write the blog post - just demonstrate correct routing." \
      --priority medium \
      --assignee-agent-id "357971bd-f509-4e8c-a147-b519cf84356d"
    ```

    Issue 2 - CMO Content Production Test (VALD-02):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Create a LinkedIn content calendar for next week" \
      --description "This is a validation test issue. The CMO should use content-strategy skill to plan LinkedIn topics for next week. Expected deliverable: A content calendar with 2 post topics, target audience for each, and which downstream agent should produce each piece. Save output to the-vault/validation-cmo-content-calendar.md" \
      --priority medium \
      --assignee-agent-id "293ac1cb-098c-4fc7-83dc-3ef8b805c8db"
    ```

    Issue 3 - Technical Writer Quality Gate Test (VALD-03):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Run quality gate pass on sample content" \
      --description "This is a validation test issue. The Technical Writer should run its 5-step quality gate pass (humaniser, de-ai-ify, copy-editing, brand voice, word count) on the following sample text:\n\nSample text: 'FourPointZero is leveraging cutting-edge AI capabilities to revolutionize the recruitment landscape. Our innovative solutions harness the power of machine learning to deliver unparalleled results for our clients in the creative technology sector.'\n\nExpected behaviour: Run humaniser and de-ai-ify to remove AI-sounding language, apply copy-editing, check brand voice against product-marketing-context.md, verify word count. Save cleaned output to the-vault/validation-tw-quality-gate.md" \
      --priority medium \
      --assignee-agent-id "3ff49ad0-539a-48e9-87fa-63f5ebcc0509"
    ```

    Issue 4 - LinkedIn Growth Director Test (VALD-06):
    ```
    source ~/.zshrc 2>/dev/null; npx paperclipai issue create -C "$COMPANY_ID" \
      --title "VALIDATION: Write LinkedIn post and cold outreach sequence about AI creative directors" \
      --description "This is a validation test issue. The LinkedIn Growth Director should: (1) Use linkedin-post-writer skill to write a LinkedIn post about how AI is changing the creative director role in VFX studios. (2) Use cold-outreach-sequence skill to create a 3-message outreach sequence targeting VFX studio heads of technology. Save post to the-vault/validation-lgd-linkedin-post.md and outreach to the-vault/validation-lgd-outreach-sequence.md" \
      --priority medium \
      --assignee-agent-id "<LINKEDIN_GROWTH_DIRECTOR_ID>"
    ```

    IMPORTANT for Issue 4: Replace <LINKEDIN_GROWTH_DIRECTOR_ID> with the actual agent ID from the fresh agent list in read_first. The LinkedIn Growth Director was created in Task 1.

    After creating all 4 issues, verify they exist:
    `source ~/.zshrc 2>/dev/null; npx paperclipai issue list -C "$COMPANY_ID" --match "VALIDATION"`
  </action>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; npx paperclipai issue list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5" --match "VALIDATION" 2>&1 | grep -c "VALIDATION" | grep -q "[4-9]" && echo "PASS: 4+ validation issues exist" || echo "FAIL: Missing validation issues"</automated>
  </verify>
  <acceptance_criteria>
    - 4 test issues created with "VALIDATION:" prefix in title
    - CEO issue assigned to CEO agent (357971bd)
    - CMO issue assigned to CMO agent (293ac1cb)
    - Technical Writer issue assigned to Technical Writer agent (3ff49ad0)
    - LinkedIn Growth Director issue assigned to LinkedIn Growth Director agent
    - All issues in todo or backlog status
  </acceptance_criteria>
  <done>4 validation test issues created and assigned to correct business stream agents.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify agents are approved and issues are assigned</name>
  <files>None - visual verification in Paperclip UI</files>
  <action>
    Present the user with the verification steps below. Wait for their confirmation before marking this plan complete.
  </action>
  <what-built>Approved all pending agents, created LinkedIn Growth Director, and created 4 validation test issues assigned to CEO, CMO, Technical Writer, and LinkedIn Growth Director.</what-built>
  <how-to-verify>
    1. Open http://localhost:3100 in your browser
    2. Check the Agents page - all 10 configured agents should show as approved/idle (not pending_approval)
    3. Check the Issues page - you should see 4 new issues with "VALIDATION:" prefix
    4. Verify each validation issue is assigned to the correct agent
    5. Confirm the LinkedIn Growth Director agent exists in the agent list
  </how-to-verify>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; npx paperclipai agent list -C "c86bff2f-e63b-4982-8a0d-aa4b50fc82a5" 2>&1 | grep -c "name=" | grep -q "1[0-1]" && echo "PASS: 10+ agents exist" || echo "FAIL: Missing agents"</automated>
  </verify>
  <done>User confirmed all 10 agents approved and 4 validation issues correctly assigned.</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All business stream agents have test issues ready for heartbeat execution in Plan 02.
</verification>

<success_criteria>
- All 10 agents in approved status (not pending_approval)
- LinkedIn Growth Director agent exists in Paperclip
- 4 VALIDATION test issues created and assigned to correct agents
- No issues in error state
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation/10-01-SUMMARY.md`
</output>
