---
phase: 17-self-healing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/code-reviewer/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/technical-writer/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/customer-success/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ux-researcher/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-content-specialist/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-outreach-specialist/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/content-producer/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/quality-reviewer/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/code-reviewer/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/technical-writer/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/customer-success/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ux-researcher/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-content-specialist/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-outreach-specialist/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/content-producer/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/quality-reviewer/MEMORY.md
autonomous: true
requirements: [HEAL-01, HEAL-03]

must_haves:
  truths:
    - "An agent in error status auto-retries from its last checkpoint on next heartbeat"
    - "Retry count is tracked and capped at 3 to prevent infinite loops"
    - "An interrupted heartbeat resumes with awareness of partial work already completed"
    - "Every auto-recovery action leaves a comment trail for debugging"
  artifacts:
    - path: "~/.paperclip/.../agents/*/HEARTBEAT.md"
      provides: "Error self-check step in Checkpoint Protocol"
      contains: "HEAL-RETRY"
    - path: "~/.paperclip/.../agents/*/MEMORY.md"
      provides: "retry_count and partial_output fields in checkpoint"
      contains: "retry_count"
  key_links:
    - from: "HEARTBEAT.md Checkpoint Protocol"
      to: "MEMORY.md Heartbeat Checkpoint"
      via: "retry_count read and increment"
      pattern: "retry_count"
    - from: "HEARTBEAT.md Checkpoint Protocol"
      to: "Issue comments"
      via: "HEAL-RETRY comment on auto-retry"
      pattern: "HEAL-RETRY"
---

<objective>
Add agent-level self-recovery to all 14 agents: error auto-retry (HEAL-01) and checkpoint resume with data preservation (HEAL-03).

Purpose: Agents currently detect errors and interruptions but stop and wait for human intervention. This plan makes them recover automatically.
Output: Updated HEARTBEAT.md (Checkpoint Protocol) and MEMORY.md (checkpoint fields) for all 14 agents.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/martynmakinson/Documents/fourpointzero/.planning/ROADMAP.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/REQUIREMENTS.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/STATE.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/phases/17-self-healing/17-RESEARCH.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/phases/16-performance-monitoring/16-01-SUMMARY.md

<interfaces>
<!-- Current Checkpoint Protocol structure (identical in all 14 agents): -->

From any agent's HEARTBEAT.md (e.g. CMO):
```markdown
## Checkpoint Protocol

At the start of each heartbeat:
1. Read `$AGENT_HOME/MEMORY.md` section "## Heartbeat Checkpoint"
2. If status is `in_progress` or `interrupted`, resume from last step
3. If status is `completed` or absent, start from step 1
```

From any agent's MEMORY.md (e.g. CMO):
```markdown
## Heartbeat Checkpoint

**Status:** completed
**Last step:** exit
**Active issue:** none
**Context:** [free text]
**Updated:** [timestamp]

### Resume Instructions
If status is `in_progress` or `interrupted`:
1. Read active issue via `GET /api/issues/{id}`
2. If issue status is `done` or `cancelled`, or assigned to another agent: clear checkpoint, start fresh
3. Attempt checkout: `POST /api/issues/{id}/checkout`
4. If 409 (checked out by another agent): clear checkpoint, start fresh
5. If checkout succeeds: continue from last step
```

All 14 agents follow this identical pattern. The agent directories are:
ceo, cmo, cto, product-owner, engineer, code-reviewer, technical-writer,
customer-success, ux-researcher, linkedin-growth-director,
linkedin-content-specialist, linkedin-outreach-specialist, content-producer, quality-reviewer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error auto-retry and data preservation to all 14 HEARTBEAT.md Checkpoint Protocols</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/HEARTBEAT.md
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/code-reviewer/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/technical-writer/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/customer-success/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ux-researcher/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-content-specialist/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-outreach-specialist/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/content-producer/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/quality-reviewer/HEARTBEAT.md
  </files>
  <action>
  For each of the 14 agents, read their HEARTBEAT.md fully, then update the Checkpoint Protocol section. The existing 3-step protocol must be expanded to include error self-check (HEAL-01) and partial work preservation (HEAL-03).

  Replace the existing Checkpoint Protocol section with this updated version (keeping the surrounding content untouched):

  ```markdown
  ## Checkpoint Protocol

  At the start of each heartbeat:
  1. Read `$AGENT_HOME/MEMORY.md` section "## Heartbeat Checkpoint"
  2. **Error self-check (HEAL-01):** Check own agent status via `GET /api/agents/me`.
     - If status is `error`:
       a. Read `retry_count` from checkpoint (default 0)
       b. If retry_count >= 3: comment `[HEAL-ERROR-LIMIT] {your agent name} hit 3 retries on issue {active issue}. Pausing for human review.` on the active issue, set checkpoint status to `paused`, and exit heartbeat
       c. If retry_count < 3: increment retry_count by 1, write updated checkpoint, comment `[HEAL-RETRY] Auto-retrying from checkpoint. Attempt {retry_count}/3.` on the active issue, then resume from last step
  3. **Partial work check (HEAL-03):** If status is `in_progress` or `interrupted`:
     a. Check if `partial_output` field has a value in the checkpoint
     b. If partial_output exists: note it in Context so the resumed step builds on prior work rather than restarting from scratch
     c. Resume from last step using Resume Instructions
  4. If status is `completed` or absent: reset retry_count to 0, clear partial_output, start from step 1
  ```

  IMPORTANT:
  - Read each HEARTBEAT.md in full BEFORE editing. Some agents have unique content (CEO has Chain-of-Command, stall detection skill reference; CMO/CTO have stall detection sections; etc.)
  - Only modify the Checkpoint Protocol section. Do NOT touch any other section.
  - The "Before each major step" and "At heartbeat exit" sub-sections remain unchanged.
  - Update the "At heartbeat exit" sub-section to add: `clear partial_output, reset retry_count to 0` alongside the existing `set checkpoint status to completed`.
  - Process all 14 agents. The full list: ceo, cmo, cto, product-owner, engineer, code-reviewer, technical-writer, customer-success, ux-researcher, linkedin-growth-director, linkedin-content-specialist, linkedin-outreach-specialist, content-producer, quality-reviewer.
  </action>
  <verify>
    <automated>grep -l "HEAL-RETRY\|HEAL-ERROR-LIMIT" ~/.paperclip/instances/default/companies/FourPointZero/agents/*/HEARTBEAT.md | wc -l | grep -q "14" && echo "PASS: 14 agents updated" || echo "FAIL: not all agents updated"</automated>
  </verify>
  <acceptance_criteria>
    - grep "HEAL-RETRY" in all 14 HEARTBEAT.md files returns matches
    - grep "HEAL-ERROR-LIMIT" in all 14 HEARTBEAT.md files returns matches
    - grep "retry_count" in all 14 HEARTBEAT.md files returns matches
    - grep "partial_output" in all 14 HEARTBEAT.md files returns matches
    - grep "Partial work check" in all 14 HEARTBEAT.md files returns matches
    - No other sections in HEARTBEAT.md were modified (spot-check 3 agents)
  </acceptance_criteria>
  <done>All 14 agents' Checkpoint Protocol sections contain error auto-retry logic with 3-attempt cap and partial work preservation on resume</done>
</task>

<task type="auto">
  <name>Task 2: Add retry_count and partial_output fields to all 14 MEMORY.md checkpoint sections</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/MEMORY.md
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/code-reviewer/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/technical-writer/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/customer-success/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ux-researcher/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-growth-director/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-content-specialist/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/linkedin-outreach-specialist/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/content-producer/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/quality-reviewer/MEMORY.md
  </files>
  <action>
  For each of the 14 agents, read their MEMORY.md fully, then update the Heartbeat Checkpoint section and Resume Instructions subsection.

  **Checkpoint section update:** Add two new fields after the `**Updated:**` line:

  ```markdown
  **Retry count:** 0
  **Partial output:** none
  ```

  These fields go AFTER `**Updated:**` and BEFORE `### Resume Instructions`.

  **Resume Instructions update:** Add a data preservation step. The current Resume Instructions have 5 steps. Insert a new step after step 1 (read active issue) and before step 2 (if issue done/cancelled). The updated Resume Instructions become:

  ```markdown
  ### Resume Instructions
  If status is `in_progress` or `interrupted`:
  1. Read active issue via `GET /api/issues/{id}`
  2. **Partial work preservation (HEAL-03):** Before resuming, check if any partial output was written to daily notes or issue comments during the interrupted heartbeat. If found, record the reference (file path or comment ID) in the `**Partial output:**` checkpoint field so the resumed step can build on it rather than starting from scratch. If no partial output found, set to `none`.
  3. If issue status is `done` or `cancelled`, or assigned to another agent: clear checkpoint, start fresh
  4. Attempt checkout: `POST /api/issues/{id}/checkout`
  5. If 409 (checked out by another agent): clear checkpoint, start fresh
  6. If checkout succeeds: continue from last step
  ```

  IMPORTANT:
  - Read each MEMORY.md in full BEFORE editing. These files are frequently modified by live agents and contain unique content per agent (CMO has FOU-241 context, CEO has X-Dept sections, etc.)
  - Only modify the Heartbeat Checkpoint section and Resume Instructions subsection. Do NOT touch Performance Metrics, X-Dept sections, or any other content.
  - Preserve whatever is currently in the Status, Last step, Active issue, Context, and Updated fields (they reflect live agent state).
  - Process all 14 agents.
  </action>
  <verify>
    <automated>grep -l "Retry count\|retry_count" ~/.paperclip/instances/default/companies/FourPointZero/agents/*/MEMORY.md | wc -l | grep -q "14" && grep -l "Partial output\|partial_output" ~/.paperclip/instances/default/companies/FourPointZero/agents/*/MEMORY.md | wc -l | grep -q "14" && echo "PASS: all 14 agents have retry_count and partial_output" || echo "FAIL: missing fields"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Retry count" in all 14 MEMORY.md files returns matches
    - grep "Partial output" in all 14 MEMORY.md files returns matches
    - grep "Partial work preservation" in all 14 MEMORY.md Resume Instructions returns matches
    - Resume Instructions now have 6 steps (was 5)
    - No Performance Metrics or other sections were modified
  </acceptance_criteria>
  <done>All 14 agents' MEMORY.md files have retry_count and partial_output checkpoint fields, and Resume Instructions include the data preservation step</done>
</task>

</tasks>

<verification>
Run these commands to verify the full plan:

```bash
AGENT_BASE=~/.paperclip/instances/default/companies/FourPointZero/agents

# HEAL-01: Error auto-retry present in all 14 agents
echo "=== HEAL-01: Error auto-retry ==="
for agent in ceo cmo cto product-owner engineer code-reviewer technical-writer customer-success ux-researcher linkedin-growth-director linkedin-content-specialist linkedin-outreach-specialist content-producer quality-reviewer; do
  grep -q "HEAL-RETRY" "$AGENT_BASE/$agent/HEARTBEAT.md" && echo "  $agent: OK" || echo "  $agent: MISSING"
done

# HEAL-03: Checkpoint resume with data preservation in all 14 agents
echo "=== HEAL-03: Data preservation ==="
for agent in ceo cmo cto product-owner engineer code-reviewer technical-writer customer-success ux-researcher linkedin-growth-director linkedin-content-specialist linkedin-outreach-specialist content-producer quality-reviewer; do
  grep -q "Partial work preservation" "$AGENT_BASE/$agent/MEMORY.md" && echo "  $agent: OK" || echo "  $agent: MISSING"
done

# Retry count field present
echo "=== Retry count field ==="
grep -c "Retry count" "$AGENT_BASE"/*/MEMORY.md
```
</verification>

<success_criteria>
- All 14 HEARTBEAT.md files contain the updated Checkpoint Protocol with error self-check (HEAL-RETRY, HEAL-ERROR-LIMIT) and partial work check
- All 14 MEMORY.md files contain retry_count and partial_output fields in the checkpoint section
- All 14 MEMORY.md files have updated Resume Instructions with the data preservation step
- No other sections in any file were modified
</success_criteria>

<output>
After completion, create `.planning/phases/17-self-healing/17-01-SUMMARY.md`
</output>
