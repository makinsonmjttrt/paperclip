---
phase: 17-self-healing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/skills/stall-detection.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
autonomous: true
requirements: [HEAL-02, HEAL-04]

must_haves:
  truths:
    - "CEO auto-clears deadlocked agents by nulling executionRunId and reassigning"
    - "Deadlock detection triggers after 1 idle cycle (not 2) because deadlocks do not self-resolve"
    - "CMO and CTO require 2 failed nudges before auto-reassigning stalled routine work"
    - "Strategic work is never auto-reassigned, only escalated"
    - "Every auto-recovery action leaves a comment trail with [HEAL-DEADLOCK], [STALL-NUDGE-1], [STALL-NUDGE-2] prefixes"
  artifacts:
    - path: "~/.paperclip/.../agents/ceo/skills/stall-detection.md"
      provides: "Deadlock auto-clear protocol"
      contains: "HEAL-DEADLOCK"
    - path: "~/.paperclip/.../agents/cmo/HEARTBEAT.md"
      provides: "2-nudge stall detection"
      contains: "STALL-NUDGE-2"
    - path: "~/.paperclip/.../agents/cto/HEARTBEAT.md"
      provides: "2-nudge stall detection"
      contains: "STALL-NUDGE-2"
  key_links:
    - from: "CEO stall-detection.md Deadlock Detection"
      to: "Paperclip API PUT /api/issues/{id}"
      via: "executionRunId null clear"
      pattern: "executionRunId.*null"
    - from: "CMO HEARTBEAT.md Stall Detection"
      to: "Issue comments"
      via: "STALL-NUDGE-1 and STALL-NUDGE-2 prefix convention"
      pattern: "STALL-NUDGE"
---

<objective>
Add organisational-level recovery: CEO deadlock auto-clear (HEAL-02) and 2-nudge stall reassignment for CMO/CTO (HEAL-04).

Purpose: The CEO currently detects deadlocks but only comments about them. CMO/CTO currently reassign after 1 nudge but HEAL-04 requires 2 nudges. This plan upgrades both.
Output: Updated CEO stall-detection.md skill and CMO/CTO HEARTBEAT.md stall detection sections.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/martynmakinson/Documents/fourpointzero/.planning/ROADMAP.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/REQUIREMENTS.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/STATE.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/phases/17-self-healing/17-RESEARCH.md
@/Users/martynmakinson/Documents/fourpointzero/.planning/phases/16-performance-monitoring/16-02-SUMMARY.md

<interfaces>
<!-- Current CEO stall-detection.md Deadlock Detection section: -->

From agents/ceo/skills/stall-detection.md:
```markdown
## Deadlock Detection

Check for issues with `executionRunId` set but agent status `idle` for 2+ cycles. This indicates a Paperclip deadlock (Issue #2516):
- Comment: `[STALL-DEADLOCK] Issue {ID} has executionRunId but agent {name} is idle. Likely deadlocked.`
- Attempt reassignment to clear the lock: `PUT /api/issues/{id}` with new assigneeAgentId.
```

<!-- Current CMO stall detection (CTO is identical structure, different reports): -->

From agents/cmo/HEARTBEAT.md section "5.5 Stall Detection":
```markdown
4. If no activity for 2+ heartbeat cycles and no existing `[STALL]` comment:
   - If `idle`: @-mention with `[STALL] Issue {ID} has had no activity for 2+ cycles. Please continue or report blockers.`
   - If `error` or `paused`: escalate to CEO

5. If you nudged in your previous heartbeat and still no progress:
   - For routine work: reassign to another available report. Comment: `[STALL-REASSIGN]`
   - For strategic work: escalate to CEO with `[STALL-ESCALATE]`
```

<!-- Comment Convention (from stall-detection.md): -->
```markdown
[STALL] -- initial nudge
[STALL-ESCALATE] -- department head escalating to CEO
[STALL-REASSIGN] -- auto-reassignment by department head
[STALL-BOARD] -- CEO escalating to board (Martyn)
[STALL-CASCADE] -- cascading stall detected
[STALL-DEADLOCK] -- executionRunId deadlock detected
```

CMO reports: Technical Writer (3ff49ad0), Customer Success (6003d629), UX Researcher (c25043f9), LinkedIn Growth Director (df0e4280)
CTO reports: Software Engineer (97307b6f), Code Reviewer (8df70cb1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade CEO deadlock detection to auto-clear protocol</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/skills/stall-detection.md
  </read_first>
  <files>~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/skills/stall-detection.md</files>
  <action>
  Read the full stall-detection.md file. Replace the existing "## Deadlock Detection" section with the upgraded auto-clear protocol below. Also update the "## Comment Convention" section to add the new prefixes.

  **Replace Deadlock Detection with:**

  ```markdown
  ## Deadlock Detection and Auto-Clear (HEAL-02)

  Check for issues with `executionRunId` set but agent status `idle`. Deadlocks do not self-resolve, so act after 1 idle cycle (not 2).

  1. Query all in-progress issues for CMO (293ac1cb), CTO (9f63e8ed), and Product Owner (f74c5796):
     `GET /api/companies/{companyId}/issues?status=in_progress`
     Filter to assigneeAgentId in [293ac1cb, 9f63e8ed, f74c5796]

  2. For each issue with `executionRunId` set (non-null):
     a. Check assigned agent status via `GET /api/agents/{agentId}`
     b. If agent is NOT `running` (i.e., `idle`, `error`, or `paused`):
        - Comment on the issue: `[HEAL-DEADLOCK] Auto-clearing stale execution lock {executionRunId}. Agent {name} was {status}.`
        - Clear the lock: `PUT /api/issues/{id}` with `{ "executionRunId": null }`
        - If the PUT fails (executionRunId: null not accepted): try reassigning the issue to a different agent and back as a workaround:
          1. `PUT /api/issues/{id}` with `{ "assigneeAgentId": "{your CEO id}" }`
          2. `PUT /api/issues/{id}` with `{ "assigneeAgentId": "{original agent id}" }`
        - If all attempts fail: comment `[STALL-BOARD] Cannot clear deadlock on issue {id}. executionRunId: {runId}. Manual intervention needed.` and move on
        - Increment the affected agent's `period_errors` by noting it in the daily notes (you cannot write to their MEMORY.md directly)

  3. Log all auto-clears in your daily notes under "## Deadlock Clears".
  ```

  **Update Comment Convention section** to add these new prefixes (keep all existing ones):

  ```markdown
  - `[HEAL-DEADLOCK]` -- auto-cleared stale execution lock (HEAL-02)
  - `[HEAL-RETRY]` -- agent auto-retrying from error (HEAL-01, added by agents themselves)
  - `[HEAL-ERROR-LIMIT]` -- agent hit 3 retry limit, paused (HEAL-01, added by agents themselves)
  - `[STALL-NUDGE-1]` -- first nudge in 2-nudge protocol (HEAL-04, added by CMO/CTO)
  - `[STALL-NUDGE-2]` -- second nudge before auto-reassign (HEAL-04, added by CMO/CTO)
  ```

  IMPORTANT:
  - Do NOT modify the "## Stall Check (Tier 2)" section or the "## Rules" section
  - The existing `[STALL-DEADLOCK]` prefix in the Comment Convention should be kept but its description updated to reference "auto-cleared" instead of "detected"
  </action>
  <verify>
    <automated>grep -c "HEAL-DEADLOCK\|executionRunId.*null\|STALL-NUDGE-1\|STALL-NUDGE-2" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/skills/stall-detection.md | grep -q "[3-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "HEAL-DEADLOCK" in stall-detection.md returns at least 2 matches (section header reference + comment template)
    - grep "executionRunId.*null" in stall-detection.md returns a match (the clear action)
    - grep "STALL-NUDGE-1" in stall-detection.md Comment Convention returns a match
    - grep "STALL-NUDGE-2" in stall-detection.md Comment Convention returns a match
    - grep "STALL-BOARD.*Cannot clear deadlock" in stall-detection.md returns a match (fallback)
    - The Stall Check (Tier 2) section is unchanged
    - The Rules section is unchanged
  </acceptance_criteria>
  <done>CEO stall-detection.md has a deadlock auto-clear protocol that clears executionRunId, with fallback reassignment, board escalation on failure, and all new comment prefixes documented</done>
</task>

<task type="auto">
  <name>Task 2: Update CMO and CTO stall detection to 2-nudge threshold</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  </files>
  <action>
  Read both CMO and CTO HEARTBEAT.md files in full. Update ONLY the "## 5.5 Stall Detection (Your Reports)" section in each file. Replace steps 4-6 with the 2-nudge protocol below. Keep steps 1-3 (query issues, check timestamps, CEO dedup check) unchanged. Keep the step numbering continuous.

  **For CMO HEARTBEAT.md**, replace steps 4 onwards with:

  ```markdown
  4. If no activity for 2+ heartbeat cycles and no existing `[STALL-NUDGE-1]` or `[STALL]` comment:
     - Check agent status: `GET /api/agents/{agentId}`
     - If `idle`: comment `[STALL-NUDGE-1] Issue {ID} inactive for 2+ cycles. Please continue or report blockers.` and @-mention the assigned agent
     - If `running`: skip (may be mid-work)
     - If `error` or `paused`: escalate to CEO with `[STALL-ESCALATE] {agent name} is in {status} state. Issue {ID} needs reassignment.`

  5. If `[STALL-NUDGE-1]` exists on the issue (from a previous heartbeat) and still no activity after 1 additional cycle:
     - Comment `[STALL-NUDGE-2] Second nudge on issue {ID}. Still inactive after first nudge. Will auto-reassign routine work if no response.` and @-mention the assigned agent

  6. If `[STALL-NUDGE-2]` exists on the issue (from a previous heartbeat) and still no activity after 1 additional cycle:
     - For routine work (content production, research, reviews): check candidate agents' status via `GET /api/agents/{agentId}` and only reassign to agents with status `idle`. Comment: `[STALL-REASSIGN] Auto-reassigned from {old} to {new} after 2 failed nudges.`
     - For strategic or irreplaceable work: escalate to CEO with `[STALL-ESCALATE] Persistent stall on issue {ID}. {agent name} unresponsive after 2 nudges. Work type requires specific agent.`

  7. Record stall findings in MEMORY.md checkpoint.
  ```

  **For CTO HEARTBEAT.md**, apply the identical pattern but with CTO-appropriate work type descriptions:

  ```markdown
  4. If no activity for 2+ heartbeat cycles and no existing `[STALL-NUDGE-1]` or `[STALL]` comment:
     - Check agent status: `GET /api/agents/{agentId}`
     - If `idle`: comment `[STALL-NUDGE-1] Issue {ID} inactive for 2+ cycles. Please continue or report blockers.` and @-mention the assigned agent
     - If `running`: skip (may be mid-work)
     - If `error` or `paused`: escalate to CEO with `[STALL-ESCALATE] {agent name} is in {status} state. Issue {ID} needs reassignment.`

  5. If `[STALL-NUDGE-1]` exists on the issue (from a previous heartbeat) and still no activity after 1 additional cycle:
     - Comment `[STALL-NUDGE-2] Second nudge on issue {ID}. Still inactive after first nudge. Will auto-reassign routine work if no response.` and @-mention the assigned agent

  6. If `[STALL-NUDGE-2]` exists on the issue (from a previous heartbeat) and still no activity after 1 additional cycle:
     - For routine work (code reviews, standard implementation): check candidate agents' status via `GET /api/agents/{agentId}` and only reassign to agents with status `idle`. Comment: `[STALL-REASSIGN] Auto-reassigned from {old} to {new} after 2 failed nudges.`
     - For architectural or irreplaceable work: escalate to CEO with `[STALL-ESCALATE] Persistent stall on issue {ID}. {agent name} unresponsive after 2 nudges. Work type requires specific agent.`

  7. Record stall findings in MEMORY.md checkpoint.
  ```

  IMPORTANT:
  - Do NOT modify any section other than "5.5 Stall Detection (Your Reports)"
  - Do NOT modify the Update Performance Metrics section or Exit section
  - The CMO's report list stays: Technical Writer (3ff49ad0), Customer Success (6003d629), UX Researcher (c25043f9), LinkedIn Growth Director (df0e4280)
  - The CTO's report list stays: Software Engineer (97307b6f), Code Reviewer (8df70cb1)
  - The sub-agent stall detection note for CMO stays: "Sub-agent stall detection is handled by team leads..."
  - Before reassigning (step 6), the agent MUST check the candidate agent's status. Do NOT reassign to an agent in `error` or `paused` status.
  </action>
  <verify>
    <automated>grep -c "STALL-NUDGE-1\|STALL-NUDGE-2\|2 failed nudges" ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md | grep -q "[3-9]" && grep -c "STALL-NUDGE-1\|STALL-NUDGE-2\|2 failed nudges" ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md | grep -q "[3-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "STALL-NUDGE-1" in CMO HEARTBEAT.md returns at least 2 matches
    - grep "STALL-NUDGE-2" in CMO HEARTBEAT.md returns at least 2 matches
    - grep "2 failed nudges" in CMO HEARTBEAT.md returns a match
    - grep "STALL-NUDGE-1" in CTO HEARTBEAT.md returns at least 2 matches
    - grep "STALL-NUDGE-2" in CTO HEARTBEAT.md returns at least 2 matches
    - grep "2 failed nudges" in CTO HEARTBEAT.md returns a match
    - The old single-nudge pattern (step 5 "If you nudged in your previous heartbeat") is gone from both files
    - Update Performance Metrics sections in both files are unchanged
  </acceptance_criteria>
  <done>CMO and CTO stall detection uses 2-nudge protocol: STALL-NUDGE-1 after 2 cycles idle, STALL-NUDGE-2 after 1 more cycle, then auto-reassign routine or escalate strategic after 1 more cycle</done>
</task>

</tasks>

<verification>
Run these commands to verify the full plan:

```bash
AGENT_BASE=~/.paperclip/instances/default/companies/FourPointZero/agents

# HEAL-02: CEO deadlock auto-clear
echo "=== HEAL-02: Deadlock auto-clear ==="
grep -c "HEAL-DEADLOCK" "$AGENT_BASE/ceo/skills/stall-detection.md"
grep -c "executionRunId.*null" "$AGENT_BASE/ceo/skills/stall-detection.md"

# HEAL-04: 2-nudge stall detection
echo "=== HEAL-04: 2-nudge protocol ==="
grep -c "STALL-NUDGE-2" "$AGENT_BASE/cmo/HEARTBEAT.md"
grep -c "STALL-NUDGE-2" "$AGENT_BASE/cto/HEARTBEAT.md"
grep -c "2 failed nudges" "$AGENT_BASE/cmo/HEARTBEAT.md"
grep -c "2 failed nudges" "$AGENT_BASE/cto/HEARTBEAT.md"

# Verify old single-nudge pattern is gone
echo "=== Old pattern removed ==="
grep -c "If you nudged in your previous heartbeat" "$AGENT_BASE/cmo/HEARTBEAT.md" "$AGENT_BASE/cto/HEARTBEAT.md"
```
</verification>

<success_criteria>
- CEO stall-detection.md has Deadlock Detection and Auto-Clear section with executionRunId clearing, fallback reassignment, and board escalation
- CEO stall-detection.md Comment Convention includes all new HEAL prefixes
- CMO HEARTBEAT.md stall detection uses 3-step nudge protocol (NUDGE-1, NUDGE-2, reassign/escalate)
- CTO HEARTBEAT.md stall detection uses 3-step nudge protocol (NUDGE-1, NUDGE-2, reassign/escalate)
- Reassignment step checks candidate agent status before reassigning
- Old single-nudge pattern removed from both CMO and CTO
</success_criteria>

<output>
After completion, create `.planning/phases/17-self-healing/17-02-SUMMARY.md`
</output>
