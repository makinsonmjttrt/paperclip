---
phase: 09-engineer-code-reviewer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md
autonomous: true
requirements: [ADPT-05]

must_haves:
  truths:
    - "document-tools.md bundle exists in engineer/skills/ containing pdf, docx, xlsx, pptx, and frontend-slides"
    - "document-tools.md follows Paperclip skill adaptation template (issue-driven input, workspace output, handover)"
    - "No interactive prompts remain in the adapted skill"
    - "Engineer AGENTS.md has a Read and follow line for document-tools"
  artifacts:
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md"
      provides: "Bundle of 5 document generation skills"
      contains: "pdf"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md"
      provides: "Skill reference for document-tools"
      contains: "document-tools"
  key_links:
    - from: "engineer/AGENTS.md"
      to: "engineer/skills/document-tools.md"
      via: "Read and follow directive"
      pattern: "Read and follow.*document-tools"
    - from: "document-tools.md"
      to: "Technical Writer"
      via: "Content handover for quality gate"
      pattern: "Technical Writer"
---

<objective>
Adapt the 5 document creation skills (pdf, docx, xlsx, pptx, frontend-slides) from Claude Code format into a single Paperclip bundle file and deploy it to the Engineer agent.

Purpose: The Engineer owns document generation but currently has no document-tools skill. The 5 source skills in ~/.claude/skills/ are Claude Code format with interactive prompts. They need adapting to Paperclip format (issue-driven input, workspace output, handover routing) and bundling into a single file per the skill ownership matrix.

Output: document-tools.md bundle deployed to engineer/skills/, AGENTS.md updated with skill reference.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/skill-ownership-matrix.md
@.planning/skill-adaptation-template.md

# Adaptation pattern reference
@.planning/phases/05-cmo-stream-skill-deployment/05-01-SUMMARY.md

<interfaces>
<!-- Source skills to adapt (Claude Code format) -->
From ~/.claude/skills/:
- pdf/SKILL.md (315 lines) -- PDF processing with pypdf, pdfplumber, reportlab, command-line tools. Has reference.md and forms.md supplements.
- docx/SKILL.md (590 lines) -- DOCX creation and editing with docx-js, pandoc, XML manipulation. Has scripts/ supplement.
- xlsx/SKILL.md (291 lines) -- Excel file creation with formatting standards, financial model colour coding. Has scripts/ supplement.
- frontend-slides/SKILL.md (184 lines) -- HTML presentations, zero-dependency, viewport-safe. Has STYLE_PRESETS.md supplement.
- pptx/ -- DOES NOT EXIST. No source skill found. Will need to be authored from scratch as a minimal entry.

<!-- Bundle template from adaptation template -->
Structure: Skill Bundle header, individual skill sections with condensed process, shared rules, input, output, handover.

<!-- Engineer existing skills (2) -->
- git-workflow.md
- pr-workflow.md
(document-tools will be the 3rd, hitting the 3-file total from ownership matrix)

<!-- AGENTS.md skill marker -->
Skills section has: `<!-- Skills are appended here by modules during company assembly -->`
Existing entries: git-workflow.md, pr-workflow.md
New entry to add: document-tools.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create document-tools.md bundle from 5 source skills</name>
  <files>~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md</files>
  <read_first>
    - .planning/skill-adaptation-template.md (bundle template structure, interactive pattern replacements, naming conventions, what NOT to change)
    - ~/.claude/skills/pdf/SKILL.md (source skill -- preserve core logic, libraries, code examples)
    - ~/.claude/skills/docx/SKILL.md (source skill -- preserve document creation patterns)
    - ~/.claude/skills/xlsx/SKILL.md (source skill -- preserve formatting standards, colour coding)
    - ~/.claude/skills/frontend-slides/SKILL.md (source skill -- preserve non-negotiables, workflow)
    - ~/.claude/skills/frontend-slides/STYLE_PRESETS.md (supplement -- inline key viewport rules)
  </read_first>
  <action>
Create a new file at `~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md` following the bundle template from the adaptation template.

**Structure the bundle as follows:**

```markdown
# Skill Bundle: Document Tools

This bundle contains 5 document generation skills. Use the one matching the issue label or file type requested.

## Skills

### PDF (pdf)
{Condensed from ~/.claude/skills/pdf/SKILL.md}
- Key libraries: pypdf (merge, split, rotate, encrypt), pdfplumber (text/table extraction), reportlab (create new PDFs)
- Command-line: pdftotext, qpdf, pdftk
- Preserve the Quick Reference table verbatim
- Preserve the ReportLab subscript/superscript warning verbatim (IMPORTANT note about Unicode glyphs rendering as black boxes)
- Remove: "Next Steps" section referencing REFERENCE.md and FORMS.md (supplements not available in Paperclip)
- Condense code examples to one per operation (merge, split, extract text, extract tables, create new PDF, OCR, watermark, password)

### DOCX (docx)
{Condensed from ~/.claude/skills/docx/SKILL.md}
- Key approach: docx-js for new documents, unpack-edit-repack for existing
- Preserve the Quick Reference table (Read/Create/Edit approach mapping)
- Preserve .doc to .docx conversion command
- Remove references to scripts/ directory (not available in Paperclip)
- Condense to: reading content, creating new documents (docx-js pattern), editing existing (XML approach)

### XLSX (xlsx)
{Condensed from ~/.claude/skills/xlsx/SKILL.md}
- Preserve ALL formatting rules verbatim (professional font, zero formula errors, preserve existing templates)
- Preserve financial model colour coding standards verbatim (blue=inputs, black=formulas, green=cross-sheet links)
- Remove references to scripts/ directory
- Condense to: requirements for outputs, core creation patterns

### PPTX (pptx)
{Author from scratch -- no source skill exists}
- Identity: PowerPoint file creation and editing
- Approach: Use python-pptx library for creating .pptx files programmatically
- Key operations: create presentation, add slides, add text/images/charts, set layouts
- Note: For HTML-based presentations, use the frontend-slides skill instead

### Frontend Slides (frontend-slides)
{Condensed from ~/.claude/skills/frontend-slides/SKILL.md + STYLE_PRESETS.md}
- Preserve ALL 5 non-negotiables verbatim (zero dependencies, viewport fit, show don't tell, distinctive design, production quality)
- Preserve workflow steps
- Inline the key viewport-safe CSS base from STYLE_PRESETS.md (the critical rem-based sizing and density limits)
- Remove interactive patterns: "exploring presentation styles with a user who does not know their design preference yet" becomes "Read the issue description for design direction. If not specified, use the Editorial Mono preset."

## Shared Rules
- All output in UK English.
- Never ask interactive questions in comments. State your interpretation and proceed.
- Save all generated files to workspace/ with descriptive filenames (e.g., `quarterly-report.pdf`, `sales-deck.pptx`).
- When the file type is ambiguous, default to: reports=PDF, editable documents=DOCX, data/financials=XLSX, presentations=PPTX (or frontend-slides for web delivery).

## Input
Read the issue description and issue labels to determine which skill to apply.
Labels: `doc:pdf`, `doc:docx`, `doc:xlsx`, `doc:pptx`, `doc:slides`

## Output
Save deliverable to: `workspace/{document-type}-{descriptive-filename}.{ext}`
Comment on the issue with a summary of what was produced.

## Handover
@-mention CTO when complete.
If the document is content-facing (client reports, pitch decks, one-pagers), @-mention Technical Writer for humaniser quality gate pass.
```

**Critical adaptation rules from the template:**
- Replace ALL interactive patterns per Section 4 of the adaptation template
- Preserve ALL core skill logic, code examples, formatting rules, and reference tables (Section 6: What NOT to Change)
- No references to REFERENCE.md, FORMS.md, scripts/, or STYLE_PRESETS.md as separate files -- inline the critical content
- Target: under 800 lines total for the bundle

**pptx skill authored from scratch** because no source exists:
- Keep it minimal (15-20 lines within the bundle)
- Reference python-pptx library
- Basic operations: create, add slides, text, images, charts
- Note that frontend-slides is preferred for web-delivered presentations
  </action>
  <verify>
    <automated>test -f ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "Skill Bundle: Document Tools" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "### PDF" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "### DOCX" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "### XLSX" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "### PPTX" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "### Frontend Slides" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "UK English" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "Technical Writer" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && grep -q "workspace/" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && ! grep -q "ask.*user" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/skills/document-tools.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at ~/.paperclip/.../agents/engineer/skills/document-tools.md
    - grep "Skill Bundle: Document Tools" returns 1 match
    - grep "### PDF" returns 1 match
    - grep "### DOCX" returns 1 match
    - grep "### XLSX" returns 1 match
    - grep "### PPTX" returns 1 match
    - grep "### Frontend Slides" returns 1 match
    - grep "UK English" returns at least 1 match
    - grep "Technical Writer" returns at least 1 match (handover)
    - grep "workspace/" returns at least 1 match (output location)
    - grep -i "ask.*user" returns 0 matches (no interactive prompts)
    - grep "pypdf\|pdfplumber\|reportlab" returns at least 1 match (PDF libraries preserved)
    - grep "docx-js\|docx" returns at least 1 match (DOCX approach preserved)
    - grep -i "colour coding\|color coding" returns at least 1 match (XLSX standards preserved)
    - grep "zero dependencies\|Zero dependencies" returns at least 1 match (frontend-slides non-negotiable preserved)
    - grep "python-pptx" returns at least 1 match (pptx from-scratch skill)
    - wc -l returns under 800
  </acceptance_criteria>
  <done>document-tools.md bundle deployed with all 5 document skills (pdf, docx, xlsx, pptx, frontend-slides). Core logic, code examples, and formatting rules preserved from sources. pptx authored from scratch. No interactive prompts. Under 800 lines. Issue-driven input, workspace output, Technical Writer handover for content-facing documents.</done>
</task>

<task type="auto">
  <name>Task 2: Add document-tools skill reference to Engineer AGENTS.md</name>
  <files>~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md</files>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md
    - .planning/skill-adaptation-template.md (Section 5: AGENTS.md Integration)
  </read_first>
  <action>
Add one line to the Engineer AGENTS.md Skills section, below the existing `pr-workflow.md` entry:

```
Read and follow: `$AGENT_HOME/skills/document-tools.md`
```

This goes after the existing two "Read and follow" lines. Do NOT modify any other part of the file.

The file may already have been modified by Plan 09-01. If so, preserve all additions from Plan 09-01 (FourPointZero Context, Skill Routing, reporting line change). Only add the single skill reference line.
  </action>
  <verify>
    <automated>grep -c "Read and follow" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md | grep -q "3" && grep -q "document-tools" ~/.paperclip/instances/default/companies/FourPointZero/agents/engineer/AGENTS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Read and follow" AGENTS.md returns exactly 3 (git-workflow + pr-workflow + document-tools)
    - grep "document-tools" AGENTS.md returns at least 1 match
    - grep "git-workflow" AGENTS.md still returns 1 match (existing preserved)
    - grep "pr-workflow" AGENTS.md still returns 1 match (existing preserved)
  </acceptance_criteria>
  <done>Engineer AGENTS.md now references all 3 skill files: git-workflow.md, pr-workflow.md, and document-tools.md. Total skill file count for Engineer: 3 (matching ownership matrix).</done>
</task>

</tasks>

<verification>
Document-tools bundle deployed and wired to Engineer:

```bash
ENGINEER_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents/engineer

# Bundle exists with all 5 skills
test -f $ENGINEER_DIR/skills/document-tools.md && echo "Bundle exists: PASS"
for skill in "### PDF" "### DOCX" "### XLSX" "### PPTX" "### Frontend Slides"; do
  grep -q "$skill" $ENGINEER_DIR/skills/document-tools.md && echo "$skill: PASS" || echo "$skill: FAIL"
done

# No interactive prompts
grep -ci "ask.*user" $ENGINEER_DIR/skills/document-tools.md
# Expected: 0

# Wired in AGENTS.md
grep -c "Read and follow" $ENGINEER_DIR/AGENTS.md
# Expected: 3

# Under 800 lines
wc -l $ENGINEER_DIR/skills/document-tools.md
# Expected: under 800

# Handover present
grep -q "Technical Writer" $ENGINEER_DIR/skills/document-tools.md && echo "Handover: PASS"
```
</verification>

<success_criteria>
- document-tools.md exists in engineer/skills/ with all 5 document skills (pdf, docx, xlsx, pptx, frontend-slides) (ADPT-05)
- Bundle follows Paperclip format: issue-driven input, workspace output, handover routing
- No interactive prompts remain (all "ask user" patterns replaced per adaptation template)
- Core skill logic preserved: PDF libraries, DOCX creation patterns, XLSX formatting standards, frontend-slides non-negotiables
- pptx skill authored from scratch using python-pptx
- Bundle under 800 lines total
- Engineer AGENTS.md has 3 "Read and follow" lines (git-workflow + pr-workflow + document-tools)
- Technical Writer handover present for content-facing documents
</success_criteria>

<output>
After completion, create `.planning/phases/09-engineer-code-reviewer/09-02-SUMMARY.md`
</output>
