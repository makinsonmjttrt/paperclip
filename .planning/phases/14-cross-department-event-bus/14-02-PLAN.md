---
phase: 14-cross-department-event-bus
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
autonomous: true
requirements: [EVNT-04, EVNT-05]

must_haves:
  truths:
    - "Draft-status x-dept handoffs sit in blocked status until the receiving department head explicitly approves"
    - "CTO can approve, reject, or request clarification on incoming business->tech handoffs"
    - "CMO can approve, reject, or request clarification on incoming tech->business handoffs"
    - "CEO can see all cross-department work currently in flight during stall detection sweep"
    - "CEO detects stalled x-dept handoffs that have been pending approval for 2+ heartbeat cycles"
  artifacts:
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md"
      provides: "Pending X-Dept Approval section for reviewing incoming tech->business handoffs"
      contains: "Pending X-Dept Approval"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md"
      provides: "Pending X-Dept Approval section for reviewing incoming business->tech handoffs"
      contains: "Pending X-Dept Approval"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md"
      provides: "Event Bus Monitoring section querying x-dept labelled issues"
      contains: "Event Bus Monitoring"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md"
      provides: "X-Dept Summary logging section"
      contains: "X-Dept Summary"
  key_links:
    - from: "cto/HEARTBEAT.md Pending X-Dept Approval"
      to: "Paperclip issues API"
      via: "query blocked issues with x-dept:pending-approval label"
      pattern: "status=blocked.*x-dept:pending-approval"
    - from: "cmo/HEARTBEAT.md Pending X-Dept Approval"
      to: "Paperclip issues API"
      via: "query blocked issues with x-dept:pending-approval label"
      pattern: "status=blocked.*x-dept:pending-approval"
    - from: "ceo/HEARTBEAT.md Event Bus Monitoring"
      to: "Paperclip issues API"
      via: "query all issues with x-dept labels"
      pattern: "x-dept"
---

<objective>
Add the approval gate for cross-department handoffs and CEO event bus monitoring oversight.

Purpose: Receiving department heads must explicitly approve incoming x-dept handoffs before any agent acts on them (EVNT-04). CEO maintains oversight of all cross-department work in flight without being in the critical path (EVNT-05).
Output: CMO and CTO HEARTBEAT.md files updated with "Pending X-Dept Approval" sections placed before "Get Assignments". CEO HEARTBEAT.md updated with "Event Bus Monitoring" section after stall detection. CEO MEMORY.md updated with X-Dept Summary logging.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cross-department-event-bus/14-RESEARCH.md
@.planning/phases/14-cross-department-event-bus/14-01-SUMMARY.md
</context>

<interfaces>
<!-- From 14-01: Cross-Department Handoff sections are now in CMO, CTO, and PO HEARTBEAT.md files.
     X-dept issues are created with status "blocked" and label "x-dept:pending-approval".
     This plan adds the receiving-side approval check and CEO monitoring. -->

Comment tags available after 14-01:
[X-DEPT] - marks handoff creation
[X-DEPT-DEFERRED] - marks rate-limited deferral

New tags this plan adds:
[X-DEPT-APPROVED] - receiving dept head approves handoff
[X-DEPT-REJECTED] - receiving dept head rejects handoff
[X-DEPT-CLARIFY] - receiving dept head needs more info
[X-DEPT-STALL] - CEO flags stalled handoff

CEO HEARTBEAT.md current structure:
- Stagger Awareness, Checkpoint Protocol
- 1. Identity and Context
- 2. Local Planning Check
- 3. Approval Follow-Up
- 4. Get Assignments
- 5. Checkout and Work
- Chain-of-Command Validation
- 6. Delegation
- 7. Fact Extraction
- 8. Exit
- CEO Responsibilities, Rules
- Backlog Health Check (Fallback)
- Assignment Check (Fallback)
- Stall Detection Check
- Data Scope Audit (lightweight)
- FPZ Delegation Logic

Agent IDs:
- CEO: (self)
- CMO: 293ac1cb
- CTO: 9f63e8ed
- Product Owner: f74c5796
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Pending X-Dept Approval sections to CMO and CTO HEARTBEAT.md</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
    - .planning/phases/14-cross-department-event-bus/14-RESEARCH.md (section: "Pattern 2: X-Dept Approval Gate")
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  </files>
  <action>
    **Step 1: Add "Pending X-Dept Approval" section to CTO HEARTBEAT.md.**

    Insert BEFORE "2. Get Assignments" as a new section "1.5 Pending X-Dept Approval". This must be checked BEFORE the CTO processes their own assignments so approved handoffs become visible in the normal assignment queue.

    Content:

    ```markdown
    ## 1.5 Pending X-Dept Approval

    Before handling your own assignments, check for cross-department handoffs awaiting your approval:

    1. Query your blocked issues:
       `GET /api/companies/{companyId}/issues?assigneeAgentId={your-id}&status=blocked`
       Filter results to issues with label `x-dept:pending-approval`.

    2. Process up to 3 pending handoffs per heartbeat:

       For each pending x-dept issue:
       a. Read the description. Check: Is the work valid? Correctly scoped for your stream?
       b. **APPROVE:** Comment `[X-DEPT-APPROVED] Accepted for tech stream. Will delegate to {report}.`
          Remove the `x-dept:pending-approval` label (keep the directional x-dept label).
          Set status to `todo`.
          Optionally reassign to the specific report who should do the work.
       c. **REJECT:** Comment `[X-DEPT-REJECTED] Reason: {specific reason}.`
          Set status to `cancelled`.
       d. **CLARIFY:** Comment `[X-DEPT-CLARIFY] Question: {specific question}.`
          Leave in `blocked` status. @-mention the originating department head.

    3. If more than 3 pending, process the highest-priority 3 and leave the rest for next cycle.
    ```

    **Step 2: Add "Pending X-Dept Approval" section to CMO HEARTBEAT.md.**

    Insert BEFORE "2. Get Assignments" as section "1.5 Pending X-Dept Approval".

    Same structure as CTO but the approval comment says "Accepted for business stream" instead of "tech stream".

    **Step 3: Renumber any affected sections** in both files to maintain sequential ordering.

    **Key design decision:** The approval section goes BEFORE "Get Assignments" because:
    - Approved handoffs change from `blocked` to `todo`, making them visible in the assignment query
    - If placed after assignments, newly approved work would not be seen until the next heartbeat cycle
    - Placing it early prevents the 2-cycle delay that would trigger CEO stall detection
  </action>
  <verify>
    <automated>
      AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents &&
      grep -c "Pending X-Dept Approval" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep -c "Pending X-Dept Approval" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep "X-DEPT-APPROVED" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep "X-DEPT-APPROVED" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep "X-DEPT-REJECTED" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep "X-DEPT-REJECTED" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep "X-DEPT-CLARIFY" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep "X-DEPT-CLARIFY" "$AGENTS_DIR/cto/HEARTBEAT.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "Pending X-Dept Approval" cmo/HEARTBEAT.md returns at least 1 match
    - grep "Pending X-Dept Approval" cto/HEARTBEAT.md returns at least 1 match
    - grep "X-DEPT-APPROVED" cmo/HEARTBEAT.md returns a match
    - grep "X-DEPT-APPROVED" cto/HEARTBEAT.md returns a match
    - grep "X-DEPT-REJECTED" both files returns matches
    - grep "X-DEPT-CLARIFY" both files returns matches
    - Approval section appears BEFORE "Get Assignments" in both files
    - Max 3 pending approvals per heartbeat documented
    - Approval removes pending-approval label but keeps directional label
  </acceptance_criteria>
  <done>
    CTO and CMO HEARTBEAT.md files each contain a "Pending X-Dept Approval" section placed before "Get Assignments". Each section supports approve (unblock to todo), reject (cancel), and clarify (leave blocked) actions with structured comment tags. Max 3 approvals per heartbeat cycle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Event Bus Monitoring to CEO HEARTBEAT.md and X-Dept Summary to CEO MEMORY.md</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
    - .planning/phases/14-cross-department-event-bus/14-RESEARCH.md (section: "Pattern 3: CEO Event Bus Monitoring")
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
  </files>
  <action>
    **Step 1: Add "Event Bus Monitoring" section to CEO HEARTBEAT.md.**

    Insert AFTER "Stall Detection Check" and BEFORE "Data Scope Audit (lightweight)". This is the natural location because event bus monitoring is an extension of the CEO's monitoring responsibilities.

    Content:

    ```markdown
    ## Event Bus Monitoring

    After stall detection, check cross-department activity:

    1. Query all non-closed issues:
       `GET /api/companies/{companyId}/issues?status=todo,in_progress,blocked,in_review`
       Filter to issues with labels containing "x-dept".

    2. For each x-dept issue found:
       - If `blocked` with `x-dept:pending-approval` for 2+ heartbeat cycles:
         Comment: `[X-DEPT-STALL] Issue #{id} awaiting approval for 2+ cycles. @{receiving-dept-head}, please review or escalate.`
       - If `in_progress` or `todo` with no activity for 2+ heartbeat cycles:
         Apply standard stall detection protocol (nudge then escalate).
       - If both department heads have rejected the same handoff topic:
         Log as a coordination failure for board review.

    3. Log summary to MEMORY.md under "## X-Dept Summary":
       ```
       active: {count in todo/in_progress}
       pending_approval: {count blocked with pending-approval}
       stalled: {count flagged this cycle}
       last_checked: {timestamp}
       ```

    4. If `pending_approval` count > 5: comment `[X-DEPT-STALL] Event bus backlog growing. {count} handoffs pending approval.` on the most recent stalled issue and @-mention both CMO and CTO.
    ```

    **Step 2: Add "X-Dept Summary" section to CEO MEMORY.md.**

    Append after the existing checkpoint section:

    ```markdown
    ## X-Dept Summary

    active: 0
    pending_approval: 0
    stalled: 0
    last_checked: none
    ```

    **Step 3: Verify the CEO event bus monitoring section references correct agent IDs.**

    The section should mention CMO (293ac1cb) and CTO (9f63e8ed) as the two department heads who manage the approval gates.

    **Do NOT modify** the CEO's existing stall detection logic. Event Bus Monitoring is a separate, additional check that runs after the existing Stall Detection Check.
  </action>
  <verify>
    <automated>
      AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents &&
      grep -c "Event Bus Monitoring" "$AGENTS_DIR/ceo/HEARTBEAT.md" &&
      grep "X-DEPT-STALL" "$AGENTS_DIR/ceo/HEARTBEAT.md" &&
      grep "X-Dept Summary" "$AGENTS_DIR/ceo/MEMORY.md" &&
      grep "pending_approval" "$AGENTS_DIR/ceo/HEARTBEAT.md" &&
      grep "pending_approval" "$AGENTS_DIR/ceo/MEMORY.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "Event Bus Monitoring" ceo/HEARTBEAT.md returns 1 match
    - grep "X-DEPT-STALL" ceo/HEARTBEAT.md returns at least 1 match
    - grep "X-Dept Summary" ceo/MEMORY.md returns 1 match
    - grep "pending_approval" ceo/HEARTBEAT.md returns at least 1 match
    - grep "pending_approval" ceo/MEMORY.md returns 1 match
    - Event Bus Monitoring section appears after Stall Detection Check
    - CEO monitors for 2+ cycle stalls on pending-approval handoffs
    - CEO logs active, pending_approval, stalled counts to MEMORY.md
  </acceptance_criteria>
  <done>
    CEO HEARTBEAT.md has "Event Bus Monitoring" section after Stall Detection Check. CEO queries all x-dept labelled issues, flags stalled handoffs pending 2+ cycles, and logs summary (active/pending/stalled counts) to MEMORY.md. CEO MEMORY.md has X-Dept Summary section with initial zero values.
  </done>
</task>

</tasks>

<verification>
Run the following to verify all EVNT-04 and EVNT-05 artifacts are in place:

```bash
AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents

# EVNT-04: Approval gate in CMO and CTO
grep -c "Pending X-Dept Approval" "$AGENTS_DIR/cmo/HEARTBEAT.md" "$AGENTS_DIR/cto/HEARTBEAT.md"
grep "X-DEPT-APPROVED\|X-DEPT-REJECTED\|X-DEPT-CLARIFY" "$AGENTS_DIR/cmo/HEARTBEAT.md" "$AGENTS_DIR/cto/HEARTBEAT.md"

# EVNT-05: CEO oversight
grep -c "Event Bus Monitoring" "$AGENTS_DIR/ceo/HEARTBEAT.md"
grep "X-DEPT-STALL" "$AGENTS_DIR/ceo/HEARTBEAT.md"
grep "X-Dept Summary" "$AGENTS_DIR/ceo/MEMORY.md"

# Full phase verification: all x-dept sections present
for agent in cmo cto ceo; do
  echo "=== $agent ==="
  grep "x-dept\|X-Dept\|X-DEPT" "$AGENTS_DIR/$agent/HEARTBEAT.md" | head -5
done
```
</verification>

<success_criteria>
- CMO and CTO HEARTBEAT.md each have "Pending X-Dept Approval" section before "Get Assignments"
- Approval gate supports three actions: approve (unblock), reject (cancel), clarify (leave blocked)
- All three structured comment tags present: [X-DEPT-APPROVED], [X-DEPT-REJECTED], [X-DEPT-CLARIFY]
- CEO HEARTBEAT.md has "Event Bus Monitoring" section after Stall Detection Check
- CEO detects and flags x-dept handoffs stalled for 2+ heartbeat cycles
- CEO MEMORY.md has X-Dept Summary section for logging active/pending/stalled counts
- No modifications to existing stall detection or delegation logic
</success_criteria>

<output>
After completion, create `.planning/phases/14-cross-department-event-bus/14-02-SUMMARY.md`
</output>
