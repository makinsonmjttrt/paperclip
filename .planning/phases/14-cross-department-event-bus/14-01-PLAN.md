---
phase: 14-cross-department-event-bus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/HEARTBEAT.md
autonomous: true
requirements: [EVNT-01, EVNT-02, EVNT-03]

must_haves:
  truths:
    - "Three x-dept labels exist: x-dept:business->tech, x-dept:tech->business, x-dept:pending-approval"
    - "CMO can create a labelled cross-department handoff issue assigned to CTO"
    - "CTO can create a labelled cross-department handoff issue assigned to CMO"
    - "Product Owner can create x-dept handoff issues to either stream head"
    - "No more than 3 x-dept issues can be created per heartbeat cycle"
    - "Handoff issues use structured templates with trigger, action, context, originating issue, urgency"
  artifacts:
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md"
      provides: "Cross-Department Handoff section with templates and rate limit check"
      contains: "Cross-Department Handoff"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md"
      provides: "Cross-Department Handoff section with templates and rate limit check"
      contains: "Cross-Department Handoff"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md"
      provides: "X-Dept Counter tracking section"
      contains: "X-Dept Counter"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md"
      provides: "X-Dept Counter tracking section"
      contains: "X-Dept Counter"
  key_links:
    - from: "cmo/HEARTBEAT.md"
      to: "Paperclip labels API"
      via: "labelIds field on issue creation"
      pattern: "x-dept:business->tech"
    - from: "cto/HEARTBEAT.md"
      to: "Paperclip labels API"
      via: "labelIds field on issue creation"
      pattern: "x-dept:tech->business"
    - from: "cmo/HEARTBEAT.md"
      to: "cmo/MEMORY.md"
      via: "rate limit counter check before handoff creation"
      pattern: "x_dept_created"
---

<objective>
Create the x-dept label convention, handoff issue templates, and rate-limited cross-department handoff protocol for CMO, CTO, and Product Owner.

Purpose: Enable department heads to trigger work in each other's streams through structured, rate-limited labelled issues instead of relying on CEO as a bottleneck.
Output: Three Paperclip labels created, CMO and CTO HEARTBEAT.md files updated with cross-department handoff sections (including 6 templates and rate limiting), MEMORY.md files updated with X-Dept Counter sections, Product Owner updated with x-dept awareness.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cross-department-event-bus/14-RESEARCH.md
@.planning/phases/13-delegation-chains/13-01-SUMMARY.md
@.planning/phases/13-delegation-chains/13-02-SUMMARY.md
</context>

<interfaces>
<!-- Key structure from current agent files. Executor should use these directly. -->

CMO HEARTBEAT.md current step order:
- Stagger Awareness
- Checkpoint Protocol
- 1. Identity and Context
- 2. Get Assignments
- 3. Checkout and Work
- 3.5 Update Department Brain
- 4. Delegation (with Direct Reports table: TW=3ff49ad0, LGD=df0e4280, CS=6003d629, UXR=c25043f9)
- 4.5 Review and Override
- 5. Handover
- Stall Detection (Your Reports)
- 6. Exit
- Rules
- FPZ Content Production Oversight

CTO HEARTBEAT.md current step order:
- Stagger Awareness
- Checkpoint Protocol
- 1. Identity and Context
- 2. Get Assignments
- 3. Checkout and Work
- 3.5 Update Department Brain
- 4. Technical Oversight
- 4.5 Delegation (with Direct Reports table: Eng=97307b6f, CR=8df70cb1, PO=f74c5796)
- 4.6 Review and Override
- 5. Handover
- Stall Detection (Your Reports)
- 6. Exit
- Rules
- FPZ Engineering Oversight (with sub-sections)

CEO Org Chart Routing Table (v2.0):
| Business stream work | CMO | 293ac1cb |
| Tech stream work | CTO | 9f63e8ed |
| Cross-stream / backlog | Product Owner | f74c5796 |

Comment tag conventions already in use:
[ROUTING], [STALL], [DELEGATED], [BRIEF-PASSTHROUGH], [APPROVED], [REVISION-NEEDED], [REASSIGN], [SCOPE]

New tags this phase adds:
[X-DEPT], [X-DEPT-DEFERRED], [X-DEPT-APPROVED], [X-DEPT-REJECTED], [X-DEPT-CLARIFY], [X-DEPT-STALL]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create x-dept labels and add handoff protocol with templates to CMO and CTO</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
    - .planning/phases/14-cross-department-event-bus/14-RESEARCH.md (sections: "Label Creation", "Pattern 1: X-Dept Handoff Issue Creation", "Code Examples")
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/HEARTBEAT.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/HEARTBEAT.md
  </files>
  <action>
    **Step 1: Create the three x-dept labels via Paperclip API.**

    Use `POST /api/companies/{companyId}/labels` three times (company ID: c86bff2f-e63b-4982-8a0d-aa4b50fc82a5):
    - `{ "name": "x-dept:business->tech", "color": "e36209" }` (orange)
    - `{ "name": "x-dept:tech->business", "color": "0e8a16" }` (green)
    - `{ "name": "x-dept:pending-approval", "color": "fbca04" }` (yellow)

    Verify labels were created: `GET /api/companies/{companyId}/labels` and note the returned label IDs.

    If the Paperclip API is not reachable from this environment, document the three curl commands in a comment block at the top of both HEARTBEAT.md files for the user to run, and use placeholder IDs `{LABEL_ID_BIZ_TO_TECH}`, `{LABEL_ID_TECH_TO_BIZ}`, `{LABEL_ID_PENDING_APPROVAL}` in the heartbeat instructions.

    **Step 2: Add "Cross-Department Handoff" section to CMO HEARTBEAT.md.**

    Insert after "4.5 Review and Override" and before "5. Handover" as section "4.6 Cross-Department Handoff".

    Content must include:
    1. Rate limit check: read MEMORY.md "## X-Dept Counter". If `x_dept_created` >= 3, add to `x_dept_deferred`, comment `[X-DEPT-DEFERRED]` on originating issue, skip.
    2. Issue creation via `POST /api/companies/{companyId}/issues` with:
       - title: `[X-DEPT] {action needed} -- triggered by {deliverable}`
       - status: `blocked`
       - assigneeAgentId: `9f63e8ed` (CTO)
       - labelIds: `[{LABEL_ID_BIZ_TO_TECH}, {LABEL_ID_PENDING_APPROVAL}]`
       - priority: match originating issue
    3. Comment on originating issue: `[X-DEPT] Cross-department handoff #{newId} created. Assigned to CTO for approval. Status: blocked (pending approval).`
    4. Increment `x_dept_created` in MEMORY.md.
    5. "Do NOT wait for handoff. Continue your own work."

    Then add "### X-Dept Templates (Business -> Tech)" with these 4 templates:
    - Positioning/Copy Update
    - Competitor Response
    - Content Gap
    - SEO/Growth Opportunity

    Each template must have fields: [X-DEPT HANDOFF] header, Trigger, Action needed, Context, Originating issue (#{parentId}), Urgency (low/medium/high).

    **Step 3: Add "Cross-Department Handoff" section to CTO HEARTBEAT.md.**

    Insert after "4.6 Review and Override" and before "5. Handover" as section "4.7 Cross-Department Handoff".

    Same structure as CMO but:
    - assigneeAgentId: `293ac1cb` (CMO)
    - labelIds: `[{LABEL_ID_TECH_TO_BIZ}, {LABEL_ID_PENDING_APPROVAL}]`

    Then add "### X-Dept Templates (Tech -> Business)" with these 2 templates:
    - Feature Launch Content Request
    - Tech Debt Escalation

    Each template must have fields: [X-DEPT HANDOFF] header, Trigger, Action needed, Context, Originating issue (#{parentId}), Urgency.

    **Step 4: Add deferred handoff processing to both CMO and CTO.**

    At the start of each "Cross-Department Handoff" section, before the rate limit check, add:
    ```
    0. Process deferred handoffs: If MEMORY.md `x_dept_deferred` is non-empty,
       re-read each originating issue. If still active (not done/cancelled),
       create the handoff (counts toward this cycle's limit). Drop stale deferrals.
    ```

    **Anti-patterns to enforce in the instructions (add as a "Rules" sub-list within each section):**
    - Never reassign existing issues across departments (always create NEW issues)
    - Never assign x-dept issues to individual contributors (always assign to other stream head)
    - Individual contributors do NOT create x-dept issues (they escalate to their dept head)
    - No-return rule: once received, the receiving department OWNS the issue

    **Renumber subsequent sections** in both files to keep step numbers sequential.
  </action>
  <verify>
    <automated>
      AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents &&
      grep -c "Cross-Department Handoff" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep -c "Cross-Department Handoff" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep -c "X-Dept Templates" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep -c "X-Dept Templates" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep -c "x_dept_created" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep -c "x_dept_created" "$AGENTS_DIR/cto/HEARTBEAT.md" &&
      grep "x-dept:business->tech" "$AGENTS_DIR/cmo/HEARTBEAT.md" &&
      grep "x-dept:tech->business" "$AGENTS_DIR/cto/HEARTBEAT.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "Cross-Department Handoff" cmo/HEARTBEAT.md returns at least 1 match
    - grep "Cross-Department Handoff" cto/HEARTBEAT.md returns at least 1 match
    - grep "X-Dept Templates" cmo/HEARTBEAT.md returns at least 1 match
    - grep "X-Dept Templates" cto/HEARTBEAT.md returns at least 1 match
    - grep "x_dept_created" cmo/HEARTBEAT.md returns at least 1 match
    - grep "x_dept_created" cto/HEARTBEAT.md returns at least 1 match
    - grep "x-dept:business->tech" cmo/HEARTBEAT.md returns a match
    - grep "x-dept:tech->business" cto/HEARTBEAT.md returns a match
    - CMO has 4 business->tech templates (Positioning, Competitor, Content Gap, SEO)
    - CTO has 2 tech->business templates (Feature Launch, Tech Debt)
    - Both files contain the no-return rule and anti-pattern rules
  </acceptance_criteria>
  <done>
    CMO HEARTBEAT.md has Cross-Department Handoff section with 4 templates, rate limit check, and anti-pattern rules. CTO HEARTBEAT.md has Cross-Department Handoff section with 2 templates, rate limit check, and anti-pattern rules. Both reference x-dept labels and assign to opposite stream head. X-dept labels either created via API or documented as curl commands for user execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add X-Dept Counter to MEMORY.md files and update Product Owner with x-dept awareness</name>
  <read_first>
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md
    - ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/HEARTBEAT.md
    - .planning/phases/14-cross-department-event-bus/14-RESEARCH.md (section: "Pattern 4: Rate Limiting via MEMORY.md Counter")
  </read_first>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cmo/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/cto/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/product-owner/HEARTBEAT.md
  </files>
  <action>
    **Step 1: Add X-Dept Counter section to CMO MEMORY.md.**

    Append after the existing "### Resume Instructions" section:

    ```markdown
    ## X-Dept Counter

    heartbeat_cycle: none
    x_dept_created: 0
    x_dept_deferred: []
    ```

    Reset rule (add as a note below the counter): "At heartbeat start, if heartbeat_cycle timestamp is from a previous cycle, reset x_dept_created to 0 and clear x_dept_deferred."

    **Step 2: Add identical X-Dept Counter section to CTO MEMORY.md.**

    Same content as Step 1.

    **Step 3: Add x-dept awareness to Product Owner HEARTBEAT.md.**

    Product Owner is cross-stream and should be able to create x-dept issues in either direction. Read the current PO HEARTBEAT.md and add a "Cross-Department Handoff" section (similar to CMO/CTO but with both directional labels available).

    The PO section must:
    - Follow the same rate limit pattern (check MEMORY.md counter, max 3 per cycle)
    - Allow assigning to either CMO (293ac1cb) or CTO (9f63e8ed) depending on work type
    - Use the appropriate directional label based on which stream the work targets
    - Always use `blocked` status with `x-dept:pending-approval` label
    - Include same anti-pattern rules (new issues only, assign to stream heads only, no-return)

    PO does NOT need handoff templates (PO creates backlog-style handoffs, not deliverable-triggered ones). Instead add a single generic template:

    ```
    **Cross-Stream Backlog Handoff:**
    > [X-DEPT HANDOFF] Cross-stream
    > **Trigger:** Backlog prioritisation decision: {decision}
    > **Action needed:** {specific action for receiving stream}
    > **Context:** {backlog context and business/tech rationale}
    > **Originating issue:** #{parentId}
    > **Urgency:** {low/medium/high}
    ```

    Also add X-Dept Counter to Product Owner MEMORY.md if it exists (create if not):
    ```markdown
    ## X-Dept Counter

    heartbeat_cycle: none
    x_dept_created: 0
    x_dept_deferred: []
    ```
  </action>
  <verify>
    <automated>
      AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents &&
      grep -c "X-Dept Counter" "$AGENTS_DIR/cmo/MEMORY.md" &&
      grep -c "X-Dept Counter" "$AGENTS_DIR/cto/MEMORY.md" &&
      grep -c "x_dept_created" "$AGENTS_DIR/cmo/MEMORY.md" &&
      grep -c "x_dept_created" "$AGENTS_DIR/cto/MEMORY.md" &&
      grep -c "Cross-Department Handoff\|x-dept" "$AGENTS_DIR/product-owner/HEARTBEAT.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "X-Dept Counter" cmo/MEMORY.md returns 1 match
    - grep "X-Dept Counter" cto/MEMORY.md returns 1 match
    - grep "x_dept_created" cmo/MEMORY.md returns at least 1 match
    - grep "x_dept_created" cto/MEMORY.md returns at least 1 match
    - grep "Cross-Department Handoff" product-owner/HEARTBEAT.md returns at least 1 match
    - Product Owner HEARTBEAT.md references both CMO (293ac1cb) and CTO (9f63e8ed) as handoff targets
  </acceptance_criteria>
  <done>
    CMO and CTO MEMORY.md files contain X-Dept Counter sections with heartbeat_cycle, x_dept_created, x_dept_deferred fields and reset rule. Product Owner HEARTBEAT.md has cross-department handoff section with generic backlog template and ability to create x-dept issues to either stream head.
  </done>
</task>

</tasks>

<verification>
Run the following to verify all EVNT-01, EVNT-02, EVNT-03 artifacts are in place:

```bash
AGENTS_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents

# EVNT-01: Label references in heartbeat files
grep "x-dept:business->tech" "$AGENTS_DIR/cmo/HEARTBEAT.md"
grep "x-dept:tech->business" "$AGENTS_DIR/cto/HEARTBEAT.md"
grep "x-dept:pending-approval" "$AGENTS_DIR/cmo/HEARTBEAT.md" "$AGENTS_DIR/cto/HEARTBEAT.md"

# EVNT-02: Templates exist
grep -c "X-Dept Templates\|X-DEPT HANDOFF" "$AGENTS_DIR/cmo/HEARTBEAT.md"
grep -c "X-Dept Templates\|X-DEPT HANDOFF" "$AGENTS_DIR/cto/HEARTBEAT.md"

# EVNT-03: Rate limiting
grep "x_dept_created.*3\|>= 3" "$AGENTS_DIR/cmo/HEARTBEAT.md" "$AGENTS_DIR/cto/HEARTBEAT.md"
grep "X-Dept Counter" "$AGENTS_DIR/cmo/MEMORY.md" "$AGENTS_DIR/cto/MEMORY.md"
```
</verification>

<success_criteria>
- Three x-dept labels either created via API or documented as curl commands for user execution
- CMO HEARTBEAT.md has "Cross-Department Handoff" section with 4 business->tech templates and rate limit check
- CTO HEARTBEAT.md has "Cross-Department Handoff" section with 2 tech->business templates and rate limit check
- Product Owner HEARTBEAT.md has cross-department handoff section with generic template
- CMO and CTO MEMORY.md files have X-Dept Counter sections
- Anti-pattern rules documented in all three handoff sections
- All step numbering in HEARTBEAT.md files is sequential and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/14-cross-department-event-bus/14-01-SUMMARY.md`
</output>
