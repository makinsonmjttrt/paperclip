---
phase: 19-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [E2E-01, E2E-02]

must_haves:
  truths:
    - "A test issue traverses CEO > CMO > LGD and produces delegation comments at each hop"
    - "A test issue triggers cross-department handoff from CMO to CTO with correct x-dept labels"
    - "CTO approves the x-dept handoff and the issue transitions from blocked to todo"
  artifacts:
    - path: "Paperclip issue (E2E-01)"
      provides: "Full delegation chain test issue with VALIDATION: prefix"
    - path: "Paperclip issue (E2E-02)"
      provides: "Cross-department handoff test issue with x-dept labels"
  key_links:
    - from: "CEO heartbeat"
      to: "CMO sub-issue"
      via: "Delegation routing table"
      pattern: "DELEGATED|ROUTING"
    - from: "CMO heartbeat"
      to: "x-dept issue for CTO"
      via: "Cross-department handoff protocol"
      pattern: "X-DEPT"
    - from: "CTO heartbeat"
      to: "x-dept approval"
      via: "Pending approval check"
      pattern: "X-DEPT-APPROVED"
---

<objective>
Prove the full delegation chain and cross-department handoff work end-to-end by creating real test issues and running agent heartbeats in sequence.

Purpose: Validates E2E-01 (4-hop delegation CEO > CMO > LGD > LCS) and E2E-02 (business-to-tech cross-department handoff via event bus labels).
Output: Test issues with documented comment trails proving delegation and handoff completion.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-end-to-end-validation/19-RESEARCH.md
@.planning/phases/17-self-healing/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks and full delegation chain test (E2E-01)</name>
  <files>No files modified - Paperclip API operations only</files>
  <read_first>
    - 19-RESEARCH.md "Agent IDs" table and "Pattern 1: Full Delegation Chain" section
    - 17-02-SUMMARY.md for stall detection comment conventions
  </read_first>
  <action>
**Pre-flight (run before any test):**
1. Load nvm: `source ~/.zshrc 2>/dev/null`
2. Set COMPANY_ID: `c86bff2f-e63b-4982-8a0d-aa4b50fc82a5`
3. Verify all agents are idle/approved: `curl -s "http://localhost:3100/api/companies/$COMPANY_ID/agents" | jq '.[] | {id: .id[0:8], name, status}'`
4. Verify x-dept labels exist: `curl -s "http://localhost:3100/api/companies/$COMPANY_ID/labels" | jq .` -- look for labels with IDs `332a701c-fd33-4cd3-9c4b-91e78ceb753f` (business->tech) and `9a4d766d-8ad7-4947-af5d-f61d56402109` (pending-approval). If missing, create them before proceeding.
5. Get full agent IDs for LinkedIn Content Specialist (5be2f2ac), LinkedIn Outreach Specialist (011d501f), Content Producer (759e8f07), Quality Reviewer (523b57db) from the agents list.

**E2E-01: Full Delegation Chain**
1. Create test issue assigned to CEO (357971bd-f509-4e8c-a147-b519cf84356d):
   ```
   npx paperclipai issue create -C "$COMPANY_ID" \
     --title "VALIDATION: Write a LinkedIn thought leadership post about AI hiring trends in VFX studios" \
     --description "E2E-01 test. Expected: CEO routes to CMO, CMO delegates to LinkedIn Growth Director, LGD delegates to LinkedIn Content Specialist (or works directly if depth limit reached). Track delegation chain via [DELEGATED] comments." \
     --priority medium \
     --assignee-agent-id 357971bd-f509-4e8c-a147-b519cf84356d
   ```
2. Record the created issue ID.
3. Run CEO heartbeat: `npx paperclipai heartbeat run -a 357971bd-f509-4e8c-a147-b519cf84356d`
4. After CEO heartbeat completes, query comments: `curl -s "http://localhost:3100/api/issues/<issueId>/comments" | jq '.[].body'`
5. Verify a sub-issue was created with assigneeAgentId = CMO (293ac1cb). Check issue list or CEO comment for sub-issue ID.
6. Run CMO heartbeat: `npx paperclipai heartbeat run -a 293ac1cb-098c-4fc7-83dc-3ef8b805c8db`
7. After CMO heartbeat, verify sub-issue created for LGD (df0e4280) with [BRIEF-PASSTHROUGH] in description.
8. Run LGD heartbeat: `npx paperclipai heartbeat run -a df0e4280`  (use full ID from pre-flight step 5)
9. After LGD heartbeat, verify either: (a) sub-issue created for LinkedIn Content Specialist, OR (b) LGD did the work directly (depth limit reached). Both are valid.
10. If LGD delegated, optionally run LinkedIn Content Specialist heartbeat.

**Record all issue IDs and comment evidence for the summary.**

Note: Each heartbeat may take 1-3 minutes. Run them sequentially (CEO first, then CMO, then LGD). If any agent enters error status, the HEAL-01 self-retry should handle it on the next heartbeat run -- re-run the heartbeat once more.
  </action>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; curl -s "http://localhost:3100/api/issues/<E2E-01-issueId>/comments" | jq '.[].body | select(contains("DELEGAT") or contains("ROUTING"))'</automated>
    Query the original E2E-01 issue and all sub-issues. At minimum 2 delegation hops must have occurred (CEO > CMO > LGD). Each hop evidenced by a comment containing [DELEGATED] or [ROUTING].
  </verify>
  <acceptance_criteria>
    - Original issue assigned to CEO has at least one comment with [DELEGATED] or [ROUTING]
    - A sub-issue exists assigned to CMO (293ac1cb)
    - A sub-sub-issue exists assigned to LGD (df0e4280)
    - Either a sub-sub-sub-issue exists assigned to LinkedIn Content Specialist OR LGD completed the work directly
    - No agents remain in error status after test completion
  </acceptance_criteria>
  <done>CEO > CMO > LGD chain completed with comment evidence at each hop. Minimum 3-hop delegation proven.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-department handoff test (E2E-02)</name>
  <files>No files modified - Paperclip API operations only</files>
  <read_first>
    - 19-RESEARCH.md "Pattern 2: Cross-Department Handoff" section
    - 19-RESEARCH.md "Label IDs" for x-dept labels
  </read_first>
  <action>
**E2E-02: Cross-Department Handoff**
1. Create test issue assigned to CMO (293ac1cb-098c-4fc7-83dc-3ef8b805c8db):
   ```
   npx paperclipai issue create -C "$COMPANY_ID" \
     --title "VALIDATION: Review SEO technical requirements and implement schema markup changes for FPZ website" \
     --description "E2E-02 test. This requires technical implementation work. Expected: CMO recognises tech dependency, creates [X-DEPT] handoff issue for CTO with x-dept:business->tech and x-dept:pending-approval labels." \
     --priority medium \
     --assignee-agent-id 293ac1cb-098c-4fc7-83dc-3ef8b805c8db
   ```
2. Record the created issue ID.
3. Run CMO heartbeat: `npx paperclipai heartbeat run -a 293ac1cb-098c-4fc7-83dc-3ef8b805c8db`
4. After CMO heartbeat, query issue list for a new issue with title starting `[X-DEPT]`:
   ```
   curl -s "http://localhost:3100/api/companies/$COMPANY_ID/issues" | jq '.[] | select(.title | startswith("[X-DEPT]")) | {id, title, status, assigneeAgentId, labels}'
   ```
5. Verify the x-dept issue has:
   - `assigneeAgentId` = CTO (9f63e8ed)
   - `status` = `blocked`
   - Labels include `x-dept:business->tech` (332a701c) and `x-dept:pending-approval` (9a4d766d)
6. Run CTO heartbeat: `npx paperclipai heartbeat run -a 9f63e8ed-328b-4eea-9b2f-30d2c1d42e1d`
7. After CTO heartbeat, verify:
   - Comment on x-dept issue contains `[X-DEPT-APPROVED]`
   - `x-dept:pending-approval` label removed
   - Status changed from `blocked` to `todo` (or `in_progress`)
   - CTO may have delegated to Engineer (97307b6f)
8. Optionally run Engineer heartbeat if CTO delegated.

**Record all issue IDs, label states, and comment evidence for the summary.**

If CMO does NOT create an x-dept handoff (routing may be ambiguous), check CMO's comment on the issue. The issue description explicitly says "requires technical implementation work" which should trigger the x-dept protocol. If it doesn't fire, note this as a finding and check CMO's HEARTBEAT.md cross-department section for the trigger conditions.
  </action>
  <verify>
    <automated>source ~/.zshrc 2>/dev/null; curl -s "http://localhost:3100/api/companies/$COMPANY_ID/issues" | jq '.[] | select(.title | startswith("[X-DEPT]")) | {id, title, status, labels}'</automated>
    An x-dept issue must exist with CTO as assignee. After CTO heartbeat, a comment with [X-DEPT-APPROVED] must be present.
  </verify>
  <acceptance_criteria>
    - CMO created a new issue with [X-DEPT] title prefix
    - The x-dept issue was assigned to CTO (9f63e8ed)
    - The x-dept issue had x-dept:business->tech label (332a701c)
    - CTO processed the x-dept issue (comment with [X-DEPT-APPROVED] or equivalent)
    - The x-dept issue status transitioned from blocked to todo or in_progress
  </acceptance_criteria>
  <done>Cross-department handoff from business stream (CMO) to tech stream (CTO) completed via event bus labels with approval workflow.</done>
</task>

</tasks>

<verification>
- E2E-01: At least 3 delegation hops evidenced by [DELEGATED] comments across issue tree
- E2E-02: x-dept issue created with correct labels, approved by CTO with [X-DEPT-APPROVED] comment
- No agents left in error status after all tests
- All test issues have VALIDATION: prefix for easy filtering
</verification>

<success_criteria>
1. E2E-01 test issue shows complete CEO > CMO > LGD delegation chain with comment evidence
2. E2E-02 test issue shows CMO > CTO cross-department handoff with label workflow and approval
3. Results documented in summary with issue IDs and pass/fail for each requirement
</success_criteria>

<output>
After completion, create `.planning/phases/19-end-to-end-validation/19-01-SUMMARY.md`
</output>
