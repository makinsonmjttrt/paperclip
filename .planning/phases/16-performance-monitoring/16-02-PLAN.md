---
phase: 16-performance-monitoring
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
  - ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
autonomous: true
requirements: [MON-02, MON-04]

must_haves:
  truths:
    - "CEO's HEARTBEAT.md contains a Company Health Summary step that reads all 14 agents' Performance Metrics and writes a summary table to daily notes"
    - "CEO's MEMORY.md contains an X-Dept Metrics section with cumulative handoff counters (created, approved, rejected, stalled)"
    - "CEO's Event Bus Monitoring step is extended to update X-Dept Metrics counters in MEMORY.md"
    - "The health summary includes per-agent output, stalls, errors, utilisation, plus event bus metrics"
  artifacts:
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md"
      provides: "Company Health Summary step and extended Event Bus Monitoring"
      contains: "Company Health Summary"
    - path: "~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md"
      provides: "X-Dept Metrics section with handoff counters"
      contains: "## X-Dept Metrics"
  key_links:
    - from: "CEO HEARTBEAT.md (Company Health Summary step)"
      to: "All 14 agents' MEMORY.md Performance Metrics sections"
      via: "Reads Performance Metrics from each agent's MEMORY.md"
      pattern: "Read Performance Metrics from all agents"
    - from: "CEO HEARTBEAT.md (Event Bus Monitoring)"
      to: "CEO MEMORY.md (X-Dept Metrics)"
      via: "Counts x-dept issues by status and writes cumulative counters"
      pattern: "handoffs_created"
    - from: "CEO HEARTBEAT.md (Company Health Summary)"
      to: "CEO daily notes"
      via: "Writes Company Health table to memory/YYYY-MM-DD.md"
      pattern: "Company Health"
---

<objective>
Add CEO-side health reporting: a daily company health summary step and structured event bus metrics counters.

Purpose: MON-02 requires the CEO to produce a daily summary of company health. MON-04 requires event bus handoff metrics to be logged. This plan makes the CEO read the per-agent metrics (created in Plan 01) and produce a company-wide view, plus track event bus throughput.

Output: Updated CEO HEARTBEAT.md with Company Health Summary step and extended Event Bus Monitoring. Updated CEO MEMORY.md with X-Dept Metrics section.
</objective>

<execution_context>
@/Users/martynmakinson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martynmakinson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-performance-monitoring/16-RESEARCH.md
@.planning/phases/16-performance-monitoring/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: every agent's MEMORY.md now contains this section -->
## Performance Metrics

### Lifetime Counters
output_count: 0
stall_count: 0
error_count: 0

### Utilisation
heartbeats_total: 0
heartbeats_with_work: 0
utilisation_pct: 0

### Current Period (daily)
period_start: 2026-04-04
period_output: 0
period_stalls: 0
period_errors: 0

<!-- CEO MEMORY.md currently has this section (will be extended in this plan) -->
## X-Dept Summary

active: 0
pending_approval: 0
stalled: 0
last_checked: 2026-04-04T13:05:00Z

<!-- CEO HEARTBEAT.md current step structure -->
## 1-7: [existing steps]
## 8. Update Performance Metrics  (added by Plan 01)
## 9. Exit  (renumbered by Plan 01)
## Stall Detection Check  (unnumbered reference)
## Event Bus Monitoring  (unnumbered reference)
## Data Scope Audit  (unnumbered reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add X-Dept Metrics section to CEO MEMORY.md and extend Event Bus Monitoring step in CEO HEARTBEAT.md</name>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
  </files>
  <read_first>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
    .planning/phases/16-performance-monitoring/16-RESEARCH.md (sections: "CEO X-Dept Metrics Extension" and "CEO Event Bus Metrics Extension")
  </read_first>
  <action>
**MEMORY.md change:**

Add a new `## X-Dept Metrics` section to the CEO's MEMORY.md. Place it AFTER the existing `## X-Dept Summary` section and BEFORE `## Performance Metrics` (which was added by Plan 01).

The exact content to insert:

```markdown
## X-Dept Metrics

handoffs_created: 0
handoffs_approved: 0
handoffs_rejected: 0
handoffs_stalled: 0
last_updated: 2026-04-04T00:00:00Z
```

Do NOT modify or remove the existing `## X-Dept Summary` section. The Summary tracks current state (active/pending/stalled counts). The Metrics track cumulative lifetime counts. Both are needed.

**HEARTBEAT.md change:**

Find the `## Event Bus Monitoring` section in CEO HEARTBEAT.md. It currently has 4 numbered items. Add a 5th item at the end of the section that updates the X-Dept Metrics counters.

Add this as item 5 in the existing Event Bus Monitoring section:

```markdown
5. Update cumulative X-Dept Metrics in MEMORY.md under "## X-Dept Metrics":
   - Count x-dept issues with status `todo` or `in_progress` created since `last_updated`: add to `handoffs_created`.
   - Count x-dept issues moved to `done` since `last_updated`: add to `handoffs_approved`.
   - Count x-dept issues `cancelled` with rejection comment since `last_updated`: add to `handoffs_rejected`.
   - Count x-dept issues flagged by stall detection since `last_updated`: add to `handoffs_stalled`.
   - Set `last_updated` to current timestamp.
```

Do NOT change items 1-4 of Event Bus Monitoring. Only append item 5.
  </action>
  <verify>
    <automated>grep -c "## X-Dept Metrics" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md && grep -c "handoffs_created:" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md && grep -c "handoffs_created" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## X-Dept Metrics" returns 1 match in CEO MEMORY.md
    - grep "handoffs_created: 0" returns 1 match in CEO MEMORY.md
    - grep "handoffs_approved: 0" returns 1 match in CEO MEMORY.md
    - grep "handoffs_rejected: 0" returns 1 match in CEO MEMORY.md
    - grep "handoffs_stalled: 0" returns 1 match in CEO MEMORY.md
    - grep "handoffs_created" returns at least 1 match in CEO HEARTBEAT.md (in the Event Bus Monitoring section)
    - The existing "## X-Dept Summary" section is still present and unchanged in CEO MEMORY.md
    - The existing Event Bus Monitoring items 1-4 are unchanged in CEO HEARTBEAT.md
  </acceptance_criteria>
  <done>CEO MEMORY.md has X-Dept Metrics section with zeroed handoff counters. CEO HEARTBEAT.md Event Bus Monitoring step includes item 5 that updates those counters on each heartbeat.</done>
</task>

<task type="auto">
  <name>Task 2: Add Company Health Summary step to CEO HEARTBEAT.md</name>
  <files>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md
  </files>
  <read_first>
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md (re-read after Task 1 edits)
    ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/MEMORY.md (for agent roster and X-Dept Metrics structure)
    .planning/phases/16-performance-monitoring/16-RESEARCH.md (section: "CEO Health Summary Heartbeat Step")
  </read_first>
  <action>
Add a new `## Company Health Summary` section to the CEO's HEARTBEAT.md. This is an unnumbered reference section (like Stall Detection Check and Event Bus Monitoring). Place it AFTER the `## Event Bus Monitoring` section and BEFORE the `## Data Scope Audit (lightweight)` section.

Also add a call to this section in the CEO's main numbered flow. Find the `## Update Performance Metrics` step (added by Plan 01, should be step 8). Insert a new step BETWEEN Update Performance Metrics and Exit. This means:
- Step 8 stays as "Update Performance Metrics"
- Step 9 becomes "Company Health Summary" (new)
- Step 10 becomes "Exit" (renumbered from 9)

The new numbered step in the main flow (insert between step 8 and Exit):

```markdown
## 9. Company Health Summary

Read and follow: the "Company Health Summary" section below.
```

The unnumbered reference section to insert after Event Bus Monitoring:

```markdown
## Company Health Summary

After event bus monitoring and before exit, generate a company health summary:

1. Read Performance Metrics from all agents listed in the Agent Roster (see MEMORY.md).
   Path pattern: `$AGENT_HOME/../{agent-dirname}/MEMORY.md`
   Agent directories: ceo, cmo, cto, product-owner, technical-writer, customer-success, ux-researcher, linkedin-growth-director, engineer, code-reviewer, linkedin-content-specialist, linkedin-outreach-specialist, content-producer, quality-reviewer

2. For each agent, extract from their Performance Metrics section:
   - `period_output` (output today)
   - `stall_count` (lifetime stalls)
   - `error_count` (lifetime errors)
   - `utilisation_pct` (utilisation percentage)

3. Query agent statuses: `GET /api/companies/{companyId}/agents` -- count how many are running, idle, error, paused.

4. Query issues completed today: `GET /api/companies/{companyId}/issues?status=done` -- filter by `updatedAt` >= today's date.

5. Read X-Dept Metrics from own MEMORY.md.

6. Write summary to `$AGENT_HOME/memory/YYYY-MM-DD.md` under `## Company Health` (overwrite if section already exists from a previous heartbeat today):

```
## Company Health

**Date:** {today}
**Agents active:** {running count} | **Stalled:** {stall count from statuses} | **Errored:** {error count from statuses} | **Issues completed today:** {done count}

| Agent | Output (today) | Stalls (lifetime) | Errors (lifetime) | Util % |
|-------|----------------|--------------------|--------------------|--------|
| {agent name} | {period_output} | {stall_count} | {error_count} | {utilisation_pct} |
...for all 14 agents...

**Event Bus:** created: {handoffs_created} | approved: {handoffs_approved} | rejected: {handoffs_rejected} | stalled: {handoffs_stalled}

> Metrics reflect last completed heartbeat per agent.
```

7. If any agent has `error_count` > 0 that increased since last summary: flag in daily notes below the table.

8. If any agent has `utilisation_pct` < 20 and `heartbeats_total` >= 5: flag as potentially underutilised (feeds Phase 18).
```

Do NOT change any existing sections. Only insert the new numbered step and the new reference section.
  </action>
  <verify>
    <automated>grep -c "Company Health Summary" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md && grep -c "period_output" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md && grep -c "Event Bus.*created" ~/.paperclip/instances/default/companies/FourPointZero/agents/ceo/HEARTBEAT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Company Health Summary" returns at least 2 matches in CEO HEARTBEAT.md (the numbered step reference + the unnumbered section heading)
    - grep "period_output" returns at least 1 match in CEO HEARTBEAT.md
    - grep "utilisation_pct" returns at least 1 match in CEO HEARTBEAT.md
    - grep "handoffs_created" returns at least 1 match in CEO HEARTBEAT.md
    - grep "underutilised" returns at least 1 match in CEO HEARTBEAT.md
    - The numbered flow has 3 final steps in order: Update Performance Metrics, Company Health Summary, Exit
    - The Exit step is renumbered to 10
    - All existing sections (Stall Detection Check, Event Bus Monitoring, Data Scope Audit) remain unchanged
  </acceptance_criteria>
  <done>CEO HEARTBEAT.md contains a Company Health Summary step that reads all 14 agents' Performance Metrics, queries agent statuses and completed issues, reads X-Dept Metrics, and writes a formatted summary table to daily notes. The summary includes event bus metrics and flags for errors and underutilisation.</done>
</task>

</tasks>

<verification>
Verify the CEO can produce a health summary by checking all the required pieces are in place:

```bash
CEO_DIR=~/.paperclip/instances/default/companies/FourPointZero/agents/ceo

echo "=== CEO MEMORY.md ==="
grep "## X-Dept Metrics" "$CEO_DIR/MEMORY.md" && echo "OK: X-Dept Metrics section exists"
grep "handoffs_created:" "$CEO_DIR/MEMORY.md" && echo "OK: handoffs_created counter exists"

echo "=== CEO HEARTBEAT.md ==="
grep "Company Health Summary" "$CEO_DIR/HEARTBEAT.md" | head -3
grep "Update Performance Metrics" "$CEO_DIR/HEARTBEAT.md" | head -1
grep "Exit" "$CEO_DIR/HEARTBEAT.md" | grep "^## [0-9]" | head -1

echo "=== Cross-check: all agents have metrics for CEO to read ==="
for a in ceo cmo cto product-owner technical-writer customer-success ux-researcher linkedin-growth-director engineer code-reviewer linkedin-content-specialist linkedin-outreach-specialist content-producer quality-reviewer; do
  grep -q "## Performance Metrics" ~/.paperclip/instances/default/companies/FourPointZero/agents/$a/MEMORY.md && echo "$a: OK" || echo "$a: MISSING"
done
```
</verification>

<success_criteria>
- CEO MEMORY.md has X-Dept Metrics section with handoffs_created, handoffs_approved, handoffs_rejected, handoffs_stalled counters
- CEO HEARTBEAT.md Event Bus Monitoring step includes item 5 updating X-Dept Metrics counters
- CEO HEARTBEAT.md has Company Health Summary step that reads all 14 agents' Performance Metrics and writes a formatted table to daily notes
- CEO HEARTBEAT.md numbered flow ends with: Update Performance Metrics -> Company Health Summary -> Exit
- Health summary includes event bus metrics from X-Dept Metrics section
- Health summary flags agents with increasing errors and low utilisation
</success_criteria>

<output>
After completion, create `.planning/phases/16-performance-monitoring/16-02-SUMMARY.md`
</output>
